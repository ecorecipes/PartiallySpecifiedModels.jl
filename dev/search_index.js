var documenterSearchIndex = {"docs":
[{"category":"section","location":"approximators/#Approximators","page":"Approximators","text":"PartiallySpecifiedModels.jl provides seven approximator types for representing unknown functions in dynamical systems. Each approximator is a callable object that maps scalar inputs to scalar outputs and is fitted as part of the model.","title":"Approximators"},{"category":"section","location":"approximators/#BSplineApproximator","page":"Approximators","text":"Cubic B-spline basis with automatic smoothing via penalized least squares. The default and most commonly used approximator.\n\nusing PartiallySpecifiedModels # hide\napprox = BSplineApproximator(:f, (0.0, 10.0), 8;\n                              initial = x -> 0.3)\n\n:f: Symbol used to access the function in the parameter struct (p.f(x))\n(0.0, 10.0): Domain of the input variable\n8: Number of evenly-spaced knots (controls flexibility)\ninitial: Function or constant for initial coefficient values\n\nThe smoothing parameter lambda is estimated automatically by LAML or GCV.","title":"BSplineApproximator"},{"category":"section","location":"approximators/#ShapeConstrainedBSplineApproximator","page":"Approximators","text":"SCOP-spline (Shape-Constrained P-spline) that enforces shape constraints on the fitted function via reparameterization of the B-spline coefficients. Supports 14 constraint types.\n\napprox_sc = ShapeConstrainedBSplineApproximator(:f, (0.0, 10.0), 8, :increasing)","title":"ShapeConstrainedBSplineApproximator"},{"category":"section","location":"approximators/#Available-constraints","page":"Approximators","text":"Constraint Description\n:increasing Monotonically increasing\n:decreasing Monotonically decreasing\n:convex Convex (curves upward)\n:concave Concave (curves downward)\n:inc_convex Increasing and convex\n:inc_concave Increasing and concave\n:dec_convex Decreasing and convex\n:dec_concave Decreasing and concave\n:positive Non-negative everywhere\n:dec_positive Decreasing and non-negative\n:inc_zero_left Increasing, zero at left endpoint\n:inc_zero_right Increasing, zero at right endpoint\n:dec_zero_left Decreasing, zero at left endpoint\n:dec_zero_right Decreasing, zero at right endpoint\n\nZero-at-endpoint constraints fix one knot coefficient to zero, reducing the parameter count by 1.\n\nFollowing Pya & Wood (2015), the monotone and monotone-plus-curvature constraints (:increasing, :decreasing, :inc_*, :dec_*) carry a free level, so the fitted function may cross zero. Use :positive or :dec_positive when positivity is required.","title":"Available constraints"},{"category":"section","location":"approximators/#SPDEApproximator","page":"Approximators","text":"Matérn SPDE (Stochastic Partial Differential Equation) approximator following Lindgren et al. (2011). Uses a finite element discretization of the Matérn SPDE to build a precision-based penalty matrix, providing an interpretable alternative to B-spline penalties.\n\napprox_spde = SPDEApproximator(:f, (0.0, 10.0), 10;\n                                nu = 1.5,\n                                range_param = 3.0,\n                                initial = x -> 0.0)\n\nnu: Matérn smoothness parameter — 0.5 (rough), 1.5 (once-differentiable, default), or 2.5 (twice-differentiable)\nrange_param: Spatial correlation length rho. Controls the scale over which the function varies. Defaults to 1/3 of the domain width.\nThe overall smoothing strength tau^2 is still estimated automatically via LAML/GCV, separately from the range.\n\nUse optimize_spde_range to select the range parameter automatically via profile GCV.","title":"SPDEApproximator"},{"category":"section","location":"approximators/#ShapeConstrainedSPDEApproximator","page":"Approximators","text":"Combines the Matérn SPDE penalty with SCOP-spline reparameterization to enforce shape constraints at mesh nodes. Supports all 14 constraint types (same as ShapeConstrainedBSplineApproximator).\n\napprox_scspde = ShapeConstrainedSPDEApproximator(:f, (0.0, 10.0), 10, :increasing;\n                                                  nu = 1.5, range_param = 3.0)\n\nConstraints are enforced at mesh nodes via a cumulative-sum reparameterization through softplus. The cubic spline interpolation between nodes may slightly overshoot; use more basis functions to reduce this.\n\ntip: Tip\nSimple constraints (:increasing, :decreasing, :concave) tend to converge more reliably than combined constraints (:inc_concave, :dec_positive) which can trap the optimizer in local optima.","title":"ShapeConstrainedSPDEApproximator"},{"category":"section","location":"approximators/#NeuralApproximator","page":"Approximators","text":"Neural network approximator using Lux.jl. Suitable for complex or high-dimensional unknown functions. Compatible with gradient-based solvers (AdamSolver, MultipleShootingSolver).\n\nimport Lux\nchain = Lux.Chain(Lux.Dense(1, 16, tanh), Lux.Dense(16, 1))\napprox_nn = NeuralApproximator(:f, chain)\n\nThe network weights are fitted as part of the optimization. Network architecture is specified as a standard Lux Chain.","title":"NeuralApproximator"},{"category":"section","location":"approximators/#GPApproximator","page":"Approximators","text":"Gaussian process approximator. Uses a GP prior over the unknown function with learnable hyperparameters (lengthscale, signal variance). Particularly useful with gradient matching solvers.\n\napprox_gp = GPApproximator(:f, (0.0, 10.0), 20)","title":"GPApproximator"},{"category":"section","location":"approximators/#COMONetApproximator","page":"Approximators","text":"Constrained Monotone Network — a neural network architecture that guarantees shape constraints by construction. Each constraint uses the architecture matching its function class: monotone constraints use positive (exp(W)) weights with saturating tanh hidden units, curvature-only constraints use a two-branch input-convex form (twice the parameters), and :positive exponentiates an unconstrained MLP.\n\napprox_comon = COMONetApproximator(:f, (0.0, 10.0), (16, 16), :increasing)","title":"COMONetApproximator"},{"category":"section","location":"approximators/#Available-constraints-2","page":"Approximators","text":"Constraint Description\n:increasing Monotonically increasing (tanh units — includes sigmoids and other monotone-nonconvex shapes)\n:decreasing Monotonically decreasing\n:convex Convex, possibly non-monotone (two-branch form, 2× parameters)\n:concave Concave, possibly non-monotone (two-branch form, 2× parameters)\n:inc_convex Increasing and convex\n:inc_concave Increasing and concave\n:dec_convex Decreasing and convex\n:dec_concave Decreasing and concave\n:positive Strictly positive output (exp of an unconstrained MLP)","title":"Available constraints"},{"category":"section","location":"approximators/#Choosing-an-Approximator","page":"Approximators","text":"Criterion BSpline ShapeConstrained SPDE Neural GP COMONet\nFew parameters ✓ ✓ ✓   \nAutomatic smoothing ✓ ✓ ✓   \nShape constraints  ✓ ✓   ✓\nInterpretable range   ✓   \nHigh flexibility    ✓ ✓ \nGradient matching ✓ ✓ ✓  ✓ \nUDE-style training    ✓  ✓\nInterpretability ✓ ✓ ✓   \n\nGeneral guidance:\n\nStart with BSplineApproximator — it works well in most cases with automatic smoothing.\nUse SPDEApproximator for an interpretable correlation-length parameter and Matérn-based smoothing.\nUse ShapeConstrainedBSplineApproximator or ShapeConstrainedSPDEApproximator when you have prior knowledge about monotonicity or convexity.\nUse NeuralApproximator when the unknown function may be complex or when using UDE-style solvers.\nUse GPApproximator with gradient matching or MAGI solvers.\nUse COMONetApproximator for neural network flexibility with monotonicity guarantees.","title":"Choosing an Approximator"},{"category":"type","location":"approximators/#PartiallySpecifiedModels.BSplineApproximator","page":"Approximators","text":"BSplineApproximator(name, domain, nknots; initial=nothing)\n\nCubic B-spline approximator with automatic smoothing penalty. Knots are evenly spaced over domain. The penalty matrix penalizes the integrated squared second derivative.\n\n\n\n\n\n","title":"PartiallySpecifiedModels.BSplineApproximator"},{"category":"type","location":"approximators/#PartiallySpecifiedModels.ShapeConstrainedBSplineApproximator","page":"Approximators","text":"ShapeConstrainedBSplineApproximator(name, domain, nknots, constraint; initial=nothing)\n\nB-spline approximator with a shape constraint enforced via the SCOP-spline reparameterization of Pya & Wood (2015).\n\nParameters are stored in unconstrained space (γ). During evaluation, knot values are computed as β = Σ * d(γ) (free level/slope components pass through linearly; the rest are softplus'd) where Σ is a constraint matrix (cumulative sum for monotonicity, second-order cumsum for convexity, etc.).\n\nFor zero-at-endpoint constraints, one knot value is fixed at 0 and nparams = nknots - 1 (Σ is q × (q-1)).\n\nArguments\n\nname: symbol for the unknown function\ndomain: (lo, hi) range of the input variable\nnknots: number of basis knots (≥ 4)\nconstraint: one of SHAPE_CONSTRAINTS\ninitial: optional initial function x -> y or constant\n\nExample\n\n# Monotone decreasing transmission rate\napprox = ShapeConstrainedBSplineApproximator(:β, (0.0, 0.15), 8, :decreasing;\n    initial=0.4)\n\n# Decreasing to zero at carrying capacity\napprox = ShapeConstrainedBSplineApproximator(:r, (0.0, 1.0), 10, :dec_zero_right;\n    initial=x -> 0.5*(1-x))\n\nReference\n\nPya, N. & Wood, S.N. (2015). Shape constrained additive models. Statistics and Computing, 25, 543–559.\n\n\n\n\n\n","title":"PartiallySpecifiedModels.ShapeConstrainedBSplineApproximator"},{"category":"type","location":"approximators/#PartiallySpecifiedModels.SPDEApproximator","page":"Approximators","text":"Matérn SPDE approximator using finite element basis functions.\n\nRepresents an unknown function as a linear combination of piecewise linear hat functions on a 1D mesh, with a Matérn SPDE penalty derived from the stochastic PDE (κ² - Δ)^(α/2) τu = W (Lindgren et al. 2011).\n\nThe penalty matrix is κ⁴C + 2κ²G + G₂ where C is the FEM mass matrix, G is the stiffness matrix, and G₂ = G C⁻¹ G approximates the biharmonic operator. The overall smoothing parameter τ² is estimated via LAML/GCV.\n\nThe correlation range ρ = √(8ν)/κ controls the effective smoothing scale, providing a physically interpretable alternative to the abstract smoothing parameter of B-spline penalties.\n\nArguments\n\nname: symbol for the unknown function\ndomain: (lo, hi) range of the input variable\nn_basis: number of mesh nodes (basis functions)\nnu: Matérn smoothness parameter (default: 1.5, i.e. Matérn 3/2)\n0.5: rough (exponential covariance, penalty = κ²C + G)\n1.5: moderate smoothness (Matérn 3/2, penalty = κ⁴C + 2κ²G + G₂)\n2.5: smooth (Matérn 5/2, penalty uses G₃ — requires α=3)\nrange_param: correlation range ρ (default: 1/3 of domain width). Larger values → smoother functions; κ = √(8ν)/ρ.\ninitial: optional initial function x -> y\n\nReferences\n\nLindgren, Rue & Lindström (2011). An explicit link between Gaussian fields and Gaussian Markov random fields. JRSS-B 73(4):423–498.\nMiller, Glennie & Seaton (2020). Understanding the SPDE approach to smoothing. JABES.\n\n\n\n\n\n","title":"PartiallySpecifiedModels.SPDEApproximator"},{"category":"type","location":"approximators/#PartiallySpecifiedModels.ShapeConstrainedSPDEApproximator","page":"Approximators","text":"ShapeConstrainedSPDEApproximator(name, domain, n_basis, constraint;\n                                 nu=1.5, range_param=nothing, initial=nothing,\n                                 penalty=:gamma_matern)\n\nSPDE (Matérn) approximator with a shape constraint enforced via the SCOP-spline reparameterization of Pya & Wood (2015).\n\nCombines the Matérn SPDE penalty (interpretable range and smoothness parameters) with shape constraints (monotonicity, positivity, convexity, etc.). Parameters are stored in unconstrained space (γ). During evaluation, mesh node values are computed as β = Σ * d(γ) (free level/slope components pass through linearly; the rest are softplus'd) where Σ is a constraint matrix, then interpolated with a cubic spline.\n\nNote: Shape constraints are enforced at mesh nodes. The cubic spline interpolation between nodes can slightly overshoot, so constraints hold approximately (not exactly) between nodes. Use more basis functions to reduce overshoot.\n\nArguments\n\nname: symbol for the unknown function\ndomain: (lo, hi) range of the input variable\nn_basis: number of mesh nodes (≥ 4)\nconstraint: one of SHAPE_CONSTRAINTS\nnu: Matérn smoothness parameter (0.5, 1.5, or 2.5)\nrange_param: correlation length ρ (default: 1/3 of domain width)\ninitial: optional initial function x -> y or constant\npenalty: :gamma_matern (default) applies the Matérn precision to the unconstrained γ as Σᵀ P_β Σ; :difference applies the Pya & Wood (2015) SCOP first-difference penalty on γ with the free level/slope in the null space. See the penalty_matrix(::ShapeConstrainedSPDEApproximator) docstring for the trade-offs — in particular, the default's λ→∞ limit is NOT a maximally smooth member of the constraint family.\n\nExample\n\n# Monotone increasing functional response (Holling-type)\napprox = ShapeConstrainedSPDEApproximator(:g, (0.0, 5.0), 10, :inc_concave;\n    nu=1.5, initial=x -> 0.1*x)\n\n\n\n\n\n","title":"PartiallySpecifiedModels.ShapeConstrainedSPDEApproximator"},{"category":"type","location":"approximators/#PartiallySpecifiedModels.NeuralApproximator","page":"Approximators","text":"NeuralApproximator(name, model; penalty_weight=0.0, domain=nothing)\n\nNeural network approximator using a Lux.jl model.\n\nArguments\n\nname: symbol for the unknown function\nmodel: a Lux.jl model (Chain, Dense, etc.)\npenalty_weight: L2 regularization weight (>0 enables LAML smoothing)\ndomain: optional (lo, hi) for input normalization to [0, 1]\n\nEvaluation and autodiff\n\nFor a Lux.Chain of Lux.Dense layers (or a bare Lux.Dense), the network is evaluated through a hand-rolled, eltype-generic MLP path (build_neural_evaluator) that is fully compatible with ForwardDiff.Dual numbers — stiff ODE solvers with autodiff Jacobians (e.g. TRBDF2(), Rosenbrock23()) and gradient-based solvers work. Other architectures fall back to Lux.apply with the input and parameters promoted to a common element type; that fallback is only as Dual-safe as the Lux kernels of the layers involved, so exotic layers may not support ForwardDiff through the dynamics.\n\n\n\n\n\n","title":"PartiallySpecifiedModels.NeuralApproximator"},{"category":"type","location":"approximators/#PartiallySpecifiedModels.GPApproximator","page":"Approximators","text":"GPApproximator(name, domain, n_inducing; kernel=:sqexp, lengthscale=nothing,\n               variance=1.0, initial=nothing)\n\nKernel-interpolation approximator. Parameters are function values at uniformly spaced inducing points, evaluated between points through the kernel weights k(x,X)K⁻¹f_X (the GP predictive-mean formula). Note this is a basis expansion, not full GP inference: no predictive variance is produced.\n\nKernel hyperparameters are handled two ways:\n\nlengthscale=nothing (default): adaptive. During LAML (after warmup iterations) and GCV fits, (lengthscale, variance) are re-estimated by empirical Bayes — maximizing the GP marginal likelihood of the current inducing values over a lengthscale × variance grid — and K/K⁻¹ are rebuilt. Because the coefficients are function values at the inducing points, re-fitting the kernel changes only the between-point behavior of the interpolant, not the values it passes through.\nlengthscale=<value>: fixed. The supplied hyperparameters are used as-is and never touched by any solver.\n\nThe LAML/GCV penalty is a spline-style second-derivative penalty on the inducing values — not the theoretical GP prior precision K⁻¹, whose narrow eigenvalue spectrum makes smoothing-parameter estimation unreliable (see penalty_matrix(::GPApproximator) in approximators.jl).\n\nKernels\n\n:sqexp  — Squared exponential: k(r) = σ² exp(-r²/(2ℓ²))\n:matern32 — Matérn 3/2: k(r) = σ²(1 + √3r/ℓ) exp(-√3r/ℓ)\n:matern52 — Matérn 5/2: k(r) = σ²(1 + √5r/ℓ + 5r²/(3ℓ²)) exp(-√5r/ℓ)\n\nArguments\n\nname: symbol for the unknown function\ndomain: (lo, hi) range of the input variable\nn_inducing: number of inducing points (like nknots for splines)\nkernel: kernel type (default :sqexp)\nlengthscale: kernel lengthscale. nothing (default) starts at domain_span / (n_inducing - 1) and adapts during the fit; an explicit value is fixed for the whole fit\nvariance: signal variance σ² (default: 1.0)\ninitial: optional initial function x -> y\n\n\n\n\n\n","title":"PartiallySpecifiedModels.GPApproximator"},{"category":"type","location":"approximators/#PartiallySpecifiedModels.COMONetApproximator","page":"Approximators","text":"COMONetApproximator <: AbstractApproximator\n\nShape-constrained neural network approximator (COMONet architecture). See COMONetApproximator(name, domain, hidden_sizes, constraint) for constructor docs.\n\n\n\n\n\n","title":"PartiallySpecifiedModels.COMONetApproximator"},{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"","title":"API Reference"},{"category":"section","location":"api/#Problem-and-Solution-Types","page":"API Reference","text":"","title":"Problem and Solution Types"},{"category":"section","location":"api/#Approximator-Types","page":"API Reference","text":"Detailed documentation for each approximator is on the Approximators page.\n\nBSplineApproximator\nShapeConstrainedBSplineApproximator\nSPDEApproximator\nShapeConstrainedSPDEApproximator\nNeuralApproximator\nGPApproximator\nCOMONetApproximator","title":"Approximator Types"},{"category":"section","location":"api/#Shape-Constraint-Constants","page":"API Reference","text":"The supported constraint symbols for the shape-constrained approximators:","title":"Shape-Constraint Constants"},{"category":"section","location":"api/#Solver-Types","page":"API Reference","text":"Detailed documentation for each solver is on the Solvers page.\n\nLAML\nGCVSolver\nCollocationLAML\nGradientMatching\nTwoStageSolver\nBNGSolver\nAdaptiveGradientMatching\nAdamSolver\nMultipleShootingSolver\nDerivativeFreeSolver\nRodeoSolver\nDaltonSolver\nMCMCSolver\nMagiSolver\nPseudoMarginalSolver\nVariationalSolver\nABCSolver\nIntegralMatchingSolver\nProfileLikelihoodSolver\nEnsembleKalmanSolver\nODINSolver\nRKHSSolver","title":"Solver Types"},{"category":"section","location":"api/#Likelihood-Types","page":"API Reference","text":"","title":"Likelihood Types"},{"category":"section","location":"api/#Core-Functions","page":"API Reference","text":"","title":"Core Functions"},{"category":"section","location":"api/#Approximator-Functions","page":"API Reference","text":"","title":"Approximator Functions"},{"category":"section","location":"api/#Bootstrap-Confidence-Intervals","page":"API Reference","text":"","title":"Bootstrap Confidence Intervals"},{"category":"section","location":"api/#Diagnostics","page":"API Reference","text":"","title":"Diagnostics"},{"category":"type","location":"api/#PartiallySpecifiedModels.PSMProblem","page":"API Reference","text":"PSMProblem\n\nA partially specified model fitting problem.\n\nFields\n\ndynamics!: right-hand side function.\nContinuous (ODE): f!(du, u, p, t) — computes derivatives du/dt\nDiscrete: f!(u_next, u, p, t) — computes next state u(t+1)\nIn both cases, p is a NamedTuple with callable unknown functions and known parameters.\nu0: initial conditions (vector or function (params) -> u0)\ntspan: time span (t0, tf)\napproximators: vector of AbstractApproximator for unknown functions\ndata_times: observation times\ndata_values: observations matrix (n_times × n_obs)\ndata_weights: weight matrix (same shape as data_values)\nobs_to_state: maps observation column j → state variable index\nknown_params: NamedTuple of known (fixed) parameters\nlikelihood: likelihood family\node_solver: ODE/discrete solver (e.g. Tsit5() for continuous, FunctionMap() for discrete, or nothing for solvers that don't integrate)\node_kwargs: additional solver keyword arguments\ndiscrete: whether this is a discrete-time model\ndelays: delay values for DDE problems (empty for ODE/discrete)\nhistory: history function h(p, t) for DDE problems (nothing for ODE)\n\n\n\n\n\n","title":"PartiallySpecifiedModels.PSMProblem"},{"category":"type","location":"api/#PartiallySpecifiedModels.PSMSolution","page":"API Reference","text":"PSMSolution\n\nResult of fitting a PSM.\n\nFields\n\nparameters: ComponentArray with sections for each approximator\nobjective: final penalized objective value\ndata_loss: unpenalized data misfit — the WEIGHTED residual sum of squares Σ wᵢⱼ (yᵢⱼ − ŷᵢⱼ)², computed by weighted_data_loss over the usable cells only (positive weight and finite datum, so one masked-out missing observation cannot turn it into NaN). This is the convention for EVERY likelihood family and every solver: no solver reports a deviance here, so for non-Gaussian families data_loss is a descriptive SSE and NOT the quantity the solver optimised (that is objective)\nedf: estimated degrees of freedom\nsmoothing_params: vector of estimated smoothing parameters λ\nfitted_values: predicted values at data times (ntimes × nobs)\nunknown_functions: Dict of name => callable evaluator\nconvergence: convergence information. For the iterative optimisers (LAML, GCVSolver, CollocationLAML, AdamSolver, MultipleShootingSolver, TwoStageSolver, IntegralMatchingSolver, BNGSolver, ODINSolver, RKHSSolver) this is a NamedTuple containing at least\nconverged::Bool — true only when a genuine stopping criterion fired (never when the loop merely exhausted its iteration budget),\niterations::Int — iterations actually performed,\nreason::Symbol — why the loop stopped: :converged_tol (tolerance criterion met), :plateau (objective stagnated over a window / no improving step existed), :maxiters (budget exhausted without a criterion firing), or :early_break (internal failure such as a simulation error or singular linear system),\nplus solver-specific extras documented on each solver type.\n\n\n\n\n\n","title":"PartiallySpecifiedModels.PSMSolution"},{"category":"type","location":"api/#PartiallySpecifiedModels.AbstractApproximator","page":"API Reference","text":"Abstract base type for unknown function approximators. Subtypes must implement:\n\nnparams(a): number of parameters\nevaluate(a, params, x): evaluate at point x given parameter vector\npenalty_matrix(a): smoothing penalty matrix (or nothing for unpenalized)\ninitial_params(a): default initial parameter vector\n\n\n\n\n\n","title":"PartiallySpecifiedModels.AbstractApproximator"},{"category":"constant","location":"api/#PartiallySpecifiedModels.SHAPE_CONSTRAINTS","page":"API Reference","text":"Supported shape constraints for B-spline approximators.\n\nBasic monotonicity:\n\n:increasing    — monotone increasing: f'(x) ≥ 0\n:decreasing    — monotone decreasing: f'(x) ≤ 0\n\nCurvature:\n\n:convex        — convex: f''(x) ≥ 0\n:concave       — concave: f''(x) ≤ 0\n\nCombined monotonicity + curvature:\n\n:inc_convex    — monotone increasing and convex\n:inc_concave   — monotone increasing and concave\n:dec_convex    — monotone decreasing and convex\n:dec_concave   — monotone decreasing and concave\n\nPositivity:\n\n:positive      — f(x) ≥ 0 everywhere\n:dec_positive  — monotone decreasing and positive\n\nZero at endpoint (nparams = nknots - 1):\n\n:inc_zero_left  — increasing with f(x_min) = 0  (SCAM: miso)\n:dec_zero_right — decreasing with f(x_max) = 0  (SCAM: mifo-like)\n:inc_zero_right — increasing with f(x_max) = 0\n:dec_zero_left  — decreasing with f(x_min) = 0\n\nNote: following Pya & Wood (2015), the monotone and combined monotone/curvature constraints carry a free level parameter (β₁ = γ₁), so e.g. an :increasing function may cross zero; use :positive or :dec_positive when positivity itself is required.\n\n\n\n\n\n","title":"PartiallySpecifiedModels.SHAPE_CONSTRAINTS"},{"category":"constant","location":"api/#PartiallySpecifiedModels.COMONET_CONSTRAINTS","page":"API Reference","text":"Supported shape constraints for COMONet approximators.\n\nCOMONet (Constrained Monotone Network) enforces shape constraints architecturally using exp(W) weights and specialized activations. Constraints are guaranteed everywhere by construction — not just at knot points.\n\nEach constraint uses the architecture that makes it the network's actual function class (a positive-weight ReLU network is always convex, so it cannot serve as a general monotone or general positive class):\n\nMonotonicity (positive weights + saturating tanh hidden units — the class includes sigmoids and other monotone-nonconvex shapes):\n\n:increasing    — f'(x) ≥ 0\n:decreasing    — f'(x) ≤ 0 (negate input)\n\nCurvature only (two-branch input-convex form g₁(x) + g₂(−x): convex and possibly non-monotone, e.g. U-shapes; twice the parameters):\n\n:convex        — f''(x) ≥ 0\n:concave       — f''(x) ≤ 0 (negated sum)\n\nMonotone + curvature (positive weights + ReLU/softplus or their negated concave forms):\n\n:inc_convex    — increasing + convex\n:inc_concave   — increasing + concave\n:dec_convex    — decreasing + convex\n:dec_concave   — decreasing + concave\n\nPositivity (unconstrained tanh MLP inside exp(·) — humps allowed):\n\n:positive      — f(x) > 0\n\n\n\n\n\n","title":"PartiallySpecifiedModels.COMONET_CONSTRAINTS"},{"category":"type","location":"api/#PartiallySpecifiedModels.AbstractLikelihood","page":"API Reference","text":"Abstract type for exponential family likelihoods used in LAML/IRLS.\n\nEach subtype must support:\n\nlog_likelihood(fam, y, mu, w): total log-likelihood\nirls_weights(fam, y, mu, w): IRLS working weights\n\n\n\n\n\n","title":"PartiallySpecifiedModels.AbstractLikelihood"},{"category":"type","location":"api/#PartiallySpecifiedModels.Gaussian","page":"API Reference","text":"Gaussian likelihood with identity link (unknown σ² profiled out in REML).\n\n\n\n\n\n","title":"PartiallySpecifiedModels.Gaussian"},{"category":"type","location":"api/#PartiallySpecifiedModels.Poisson","page":"API Reference","text":"Poisson likelihood, fitted on the response scale (identity link).\n\n\n\n\n\n","title":"PartiallySpecifiedModels.Poisson"},{"category":"type","location":"api/#PartiallySpecifiedModels.NegativeBinomial","page":"API Reference","text":"Negative Binomial likelihood, fitted on the response scale (identity link).\n\n\n\n\n\n","title":"PartiallySpecifiedModels.NegativeBinomial"},{"category":"type","location":"api/#PartiallySpecifiedModels.TruncatedNormal","page":"API Reference","text":"TruncatedNormal(lower=0.0, sigma=1.0)\n\nTruncated Normal likelihood for non-negative continuous data.  The distribution is a Normal(μ, σ²) truncated to [lower, ∞).  Scale parameter σ is fixed (not profiled like the Gaussian σ²).\n\nUseful for continuous measurements that are bounded below (e.g., population densities, concentrations).\n\nFields\n\nlower::Float64: lower truncation point (default 0.0)\nsigma::Float64: standard deviation (default 1.0)\n\n\n\n\n\n","title":"PartiallySpecifiedModels.TruncatedNormal"},{"category":"type","location":"api/#PartiallySpecifiedModels.CustomLikelihood","page":"API Reference","text":"CustomLikelihood(loglik_scalar)\n\nUser-defined likelihood. loglik_scalar(y, μ) returns scalar log-likelihood for one observation. IRLS weights derived via ForwardDiff.\n\n\n\n\n\n","title":"PartiallySpecifiedModels.CustomLikelihood"},{"category":"function","location":"api/#CommonSolve.solve","page":"API Reference","text":"SciMLBase.solve(prob::PSMProblem, alg::LAML)\n\nFit a partially specified model using IRLS with LAML smoothing.\n\nAlgorithm\n\nFor each IRLS iteration:\n\nEvaluate model and compute FD Jacobian\nForm pseudodata z = y - f + J*β\nSolve penalized least squares (augmented system)\nStep contraction (backtracking)\nRe-estimate smoothing parameters via Fellner-Schall + Newton\n\nReturns a PSMSolution. sol.convergence is a NamedTuple (V_beta, sigma2, converged, iterations, reason, laml_failures) — see the LAML and PSMSolution docstrings for the key taxonomy.\n\n\n\n\n\nsolve(prob::PSMProblem, alg::CollocationLAML)\n\nFit a partially specified model using generalised profiling (collocation). State trajectories are represented as spline curves and optimised jointly with the unknown-function coefficients while enforcing the ODE/map as a soft constraint.\n\nAlgorithm\n\nInitialise state spline coefficients from smoothed data.\nOuter loop: update unknown-function parameters via LAML-penalised least squares with Fellner–Schall smoothing parameter estimation.\nInner loop: refine state spline coefficients by minimising a combined data-fit + ODE-fidelity + roughness-penalty objective.\nUse a continuation schedule on the ODE-fidelity weight to gradually tighten the dynamic constraint.\n\nReferences\n\nRamsay et al. (2007), \"Parameter estimation for differential equations: a generalized smoothing approach\", JRSS-B.\n\nReturns\n\nPSMSolution with fitted parameters, trajectory, and unknown functions. sol.convergence is a NamedTuple (ode_compliance, lambda_ode_final, converged, iterations, reason, iterations_total) — see the CollocationLAML docstring for the key taxonomy.\n\n\n\n\n\nsolve(prob::PSMProblem, alg::GradientMatching)\n\nFit a partially specified model using the smooth-then-match approach. Observed data are first smoothed to estimate state trajectories and their derivatives; the unknown-function parameters are then found by matching the smoothed derivatives to the model right-hand side.\n\nAlgorithm\n\nSmooth each observed state with cubic splines (continuous) or compute forward differences (discrete).\nBuild a gradient-matching objective: ∑ₜ ‖x′(t) − f(x(t), uf(t; β))‖².\nMinimise with Optim.NelderMead (or user-specified method).\nReconstruct the unknown functions at the fitted parameters.\n\nReferences\n\nVarah (1982), \"A Spline Least Squares Method for Numerical Parameter Estimation in Differential Equations\", SIAM J. Sci. Stat. Comput.\n\nReturns\n\nPSMSolution with fitted parameters, trajectory, and unknown functions.\n\n\n\n\n\nsolve(prob::PSMProblem, alg::AdamSolver)\n\nFit a partially specified model using the Adam optimizer with automatic differentiation through the ODE/map solver via ForwardDiff.jl.\n\nAlgorithm\n\nInitialize neural-network or spline parameters.\nDefine a differentiable loss: simulate the model, compute weighted residuals, and optionally add smoothing penalties.\nIterate Adam updates on the full parameter vector with learning-rate scheduling and optional gradient clipping.\nReturn the parameters at the lowest observed loss.\n\nReferences\n\nKingma & Ba (2015), \"Adam: A Method for Stochastic Optimization\", ICLR.\nRackauckas et al. (2020), \"Universal Differential Equations\", arXiv:2001.04385.\n\nReturns\n\nPSMSolution with fitted parameters, trajectory, and unknown functions. sol.convergence is a NamedTuple (optimizer, method, converged, iterations, reason, final_grad_norm) — see the AdamSolver docstring for the key taxonomy and the guarded plateau criterion.\n\n\n\n\n\nsolve(prob::PSMProblem, alg::MultipleShootingSolver)\n\nFit a partially specified model using multiple shooting. The time span is divided into intervals, each with its own initial condition; a combined objective penalises data misfit and continuity gaps between intervals.\n\nAlgorithm\n\nPartition the time span into n_intervals sub-intervals (integer boundaries for discrete-time models).\nIntroduce free initial conditions at each interval boundary, optimized jointly with the model parameters.\nForm the augmented Lagrangian: data-fit loss + v'h + (ρ/2)‖h‖² over the continuity gaps h, with multiplier updates v ← v + ρh. The data-fit loss follows prob.likelihood via loss=:auto (Gaussian → weighted SSE, Poisson → weighted negative log-likelihood kernel, matching AdamSolver; other families error). A fixed smoothing penalty penalty_weight · Σₖ βₖ'Sₖβₖ is added when penalty_weight > 0.\nMinimise each subproblem with L-BFGS (ForwardDiff gradients through the ODE/map solve), escalating ρ while gaps stagnate.\nReturn the final iterate (which best satisfies the continuity constraints); fitted values are produced by a final single-shoot simulation at those parameters.\n\nDDE problems are not supported (segment initial states cannot supply the delayed history).\n\nReferences\n\nBock & Plitt (1984), \"A Multiple Shooting Algorithm for Direct Solution of Optimal Control Problems\", IFAC Proceedings.\n\nReturns\n\nPSMSolution with fitted parameters, trajectory, and unknown functions. objective is the final single-shoot data-fit loss in the training metric (weighted SSE for :mse, the weighted Poisson NLL kernel for :poisson) plus the smoothing penalty when penalty_weight > 0; data_loss is always the descriptive weighted SSE. sol.convergence is a NamedTuple (optimizer, method, n_intervals, converged, iterations, reason, max_gap, rho_final) — see the MultipleShootingSolver docstring for the key taxonomy.\n\n\n\n\n\nsolve(prob::PSMProblem, alg::AdaptiveGradientMatching)\n\nFit a partially specified model using adaptive gradient matching with a product-of-experts Gaussian process formulation. A GP prior on the state is combined with a GP-based likelihood derived from the ODE to form a joint posterior, avoiding explicit numerical integration.\n\nAlgorithm\n\nFit a GP to each observed state to obtain smoothed trajectories and derivative statistics (mean and covariance).\nConstruct the product-of-experts posterior by combining the data GP with the ODE-implied GP likelihood.\nOptimise unknown-function parameters by maximising the joint marginal likelihood with respect to the combined model.\nOptionally re-estimate GP hyperparameters (γ) to adapt the state prior.\n\nWith n_samples > 0 the MAP fit is replaced by the paper's tempered population-MCMC sampler over states, parameters, and mismatch variances (see the AdaptiveGradientMatching docstring).\n\nReferences\n\nDondelinger et al. (2013), \"ODE parameter inference using adaptive gradient matching with Gaussian processes\", AISTATS.\n\nReturns\n\nPSMSolution with fitted parameters, trajectory, and unknown functions.\n\n\n\n\n\nsolve(prob::PSMProblem, alg::RodeoSolver)\n\nFit a partially specified model using the RODEO probabilistic-numerics solver (named after Wu & Lysy's rodeo package, the algorithmic reference for this implementation). An integrated Brownian motion (IBM) prior is placed over the state and Kalman filtering/smoothing is used to condition on both the observations and the ODE constraints.\n\nAlgorithm\n\nDiscretise the time span on a fine grid (n_steps points).\nSet up the IBM prior of order n_deriv for each state variable.\nForward Kalman filter: propagate the IBM prior and assimilate data observations and ODE residual pseudo-observations.\nRauch–Tung–Striebel backward smoother to obtain the posterior state.\nOptimise unknown-function parameters by maximising the marginal log-likelihood of the Kalman filter.\n\nReferences\n\nTronarp et al. (2022), \"Fenrir: Physics-Enhanced Regression for Initial Value Problems\", ICML.\n\nReturns\n\nPSMSolution with fitted parameters, trajectory, and unknown functions.\n\nMasked data\n\nThis solver does NOT support masked observations and raises an error if any data cell is masked (data_weights == 0 or a non-finite data_values entry). Its likelihood is evaluated inside a Kalman / particle recursion with no per-cell mask: honouring a mask means skipping the FILTER UPDATE, not merely the density term, for the masked cells. Left unguarded the masked cells corrupt the filter state while the run still looks like an ordinary converged fit, so it fails loudly instead. Drop the masked rows from data_times/data_values, or use one of the masking-capable solvers (LAML, GCVSolver, CollocationLAML, GradientMatching, TwoStageSolver, BNGSolver, ODINSolver, RKHSSolver, IntegralMatchingSolver, AdamSolver, MultipleShootingSolver, DerivativeFreeSolver, MCMCSolver, MagiSolver, VariationalSolver, ABCSolver, ProfileLikelihoodSolver).\n\n\n\n\n\nsolve(prob::PSMProblem, alg::MCMCSolver)\n\nFit a partially specified model using Markov Chain Monte Carlo sampling. Supports Hamiltonian Monte Carlo (HMC), the No-U-Turn Sampler (NUTS), and Metropolis–Hastings (MH), providing full posterior distributions over the unknown-function parameters.\n\nThe observation model follows prob.likelihood. Gaussian data get a sampled (or fixed, via obs_sigma) noise parameter σ; Poisson, NegativeBinomial, TruncatedNormal, and CustomLikelihood use the family's pointwise log-likelihood with data_weights multiplied in and sample no σ nuisance (dispersion parameters such as NegBin's θ are fixed in the family object). Passing obs_sigma with a non-Gaussian family errors.\n\nAlgorithm\n\nInitialise parameters and define the log-posterior (log-likelihood + optional smoothing-penalty prior).\nRun the selected MCMC sampler (AdvancedMH.jl) for n_samples iterations with n_warmup adaptation/burn-in steps.\nCompute posterior mean parameters and reconstruct trajectories.\nReturn the full chain alongside point estimates.\n\nReferences\n\nHoffman & Gelman (2014), \"The No-U-Turn Sampler\", JMLR.\nNeal (2011), \"MCMC using Hamiltonian dynamics\", Handbook of MCMC.\n\nReturns\n\nPSMSolution with fitted parameters, trajectory, unknown functions, and the full MCMC chain in sol.extras[:chain].\n\n\n\n\n\nsolve(prob::PSMProblem, alg::MagiSolver)\n\nFit a partially specified model using MAGI (MAnifold-constrained Gaussian process Inference; Yang, Wong & Kou 2021). A Matérn-3/2 GP prior is placed on each state; the GP-implied derivative is constrained to the ODE vector field through the conditional covariance K* = ''K − 'K C⁻¹ ('K)ᵀ, and the states X(I) are sampled jointly with the unknown-function parameters θ via NUTS.\n\nGaussian likelihoods only: the manifold-constrained posterior's data term is a Gaussian quadratic form on the state grid, so non-Gaussian prob.likelihood errors at entry. Observation noise is per state component: sigma (explicit SDs) takes priority over obs_var (shared variance; specifying both errors), which takes priority over auto-estimation. data_weights scale each observation's contribution; weight-zero entries are excluded like NaN.\n\nReferences\n\nYang, Wong & Kou (2021), \"Inference of dynamic systems from noisy and sparse data via manifold-constrained Gaussian processes\", PNAS 118(15).\n\nReturns\n\nPSMSolution; convergence.chains holds the posterior samples of θ. objective is minus the mean log-posterior over the retained draws (the only scalar objective a sampler has; negated for the package-wide lower-is-better convention) and data_loss is the usual mask-aware weighted_data_loss at the posterior-mean trajectory. convergence also carries converged/reason; NUTS runs a fixed budget with no stopping criterion, so these are always false/:maxiters — judge the sampler by the R̂ and ESS of convergence.chains.\n\nMasked observations (NaN value and/or zero weight) are supported: they are dropped from the data term, the GP hyperparameter fit and the state initialization.\n\n\n\n\n\nsolve(prob::PSMProblem, alg::BNGSolver)\n\nFit a partially specified model by ensemble Bayesian gradient matching (Bonnaffé & Coulson 2023).\n\nAlgorithm\n\nSmooth each observed series with a penalized (GCV) spline; keep the fitted values and residuals.\nBuild k_obs observation ensembles: the first is the original data, the rest add column-wise resampled residuals to the smoother fit (residual bootstrap). Re-smooth each to get state and derivative targets.\nFor each ensemble, run k_proc fits from perturbed initialisations (fresh random weights for neural approximators, jittered coefficients otherwise), minimizing the variance-marginalized log-posterior\nL(β) = (n/2)·log(1 + SSR/2) + (|β|/2)·log(1 + Σ(β/prior_sd)²/2)\n       + λ_smooth · Σ βₖᵀ S βₖ,\nwhere SSR is the gradient-matching sum of squares. Marginalizing the observation and prior variances (rather than fixing them) is the \"Bayesian regularisation\" of the cited paper: the two log terms self-balance, so no noise variance needs to be supplied.\nThe k_obs × k_proc fits form the posterior ensemble. Reported unknown functions are ensemble means; sol.convergence.ensemble_std maps each function name to a pointwise standard-deviation function, and sol.params/sol.objective come from the best (lowest-loss) member.\n\nReturns\n\nPSMSolution. Fitted values re-simulate the ODE/map with the best member's parameters. sol.convergence carries the honest-convergence keys (converged, iterations, reason) plus per-member member_converged — see the BNGSolver docstring for the key taxonomy.\n\n\n\n\n\nsolve(prob::PSMProblem, alg::DaltonSolver)\n\nFit a partially specified model using the DALTON (Data-Adaptive Likelihood with Transformed ObservatioNs) probabilistic ODE solver of Wu & Lysy (2024). The data-conditional likelihood logp(Y|Z) ≈ logp(Y,Z) − logp(Z) is evaluated with two joint Kalman-filter passes, each linearized about its own predicted means — the joint pass's linearization is informed by the assimilated data (the method's defining data-adaptivity), the marginal pass's by the ODE alone.\n\nAlgorithm\n\nSet up the IBM prior of order n_deriv for each state variable.\nMarginal pass: ODE-only joint filter giving logp(Z), EKF-linearized about its own trajectory, and the quasi-MLE global diffusion σ̂².\nBoth evidences are then taken at that single calibrated diffusion (as probsolve_filter already does for Fenrir); without this the uncalibrated quadratic ODE-defect penalty makes the difference in step 5 unbounded above — see _dalton_loglik.\nJoint pass: filter assimilating both the ODE pseudo-observations and the data, re-interrogating the ODE at each data-informed predicted mean, giving logp(Y,Z).\nlogp(Y|Z) ≈ logp(Y,Z) − logp(Z); optimize the parameters.\n\nReferences\n\nWu, M. & Lysy, M. (2024), \"Data-adaptive probabilistic likelihood approximation for ordinary differential equations\", AISTATS.\n\nReturns\n\nPSMSolution with fitted parameters, trajectory, and unknown functions.\n\nMasked data\n\nThis solver does NOT support masked observations and raises an error if any data cell is masked (data_weights == 0 or a non-finite data_values entry). Its likelihood is evaluated inside a Kalman / particle recursion with no per-cell mask: honouring a mask means skipping the FILTER UPDATE, not merely the density term, for the masked cells. Left unguarded the masked cells corrupt the filter state while the run still looks like an ordinary converged fit, so it fails loudly instead. Drop the masked rows from data_times/data_values, or use one of the masking-capable solvers (LAML, GCVSolver, CollocationLAML, GradientMatching, TwoStageSolver, BNGSolver, ODINSolver, RKHSSolver, IntegralMatchingSolver, AdamSolver, MultipleShootingSolver, DerivativeFreeSolver, MCMCSolver, MagiSolver, VariationalSolver, ABCSolver, ProfileLikelihoodSolver).\n\n\n\n\n\nsolve(prob::PSMProblem, alg::PseudoMarginalSolver)\n\nFit a partially specified model using pseudo-marginal MCMC. A probabilistic ODE solver provides an UNBIASED, stochastic estimate of the marginal likelihood (Monte-Carlo over the solver posterior via FFBS), which drives a random-walk Metropolis sampler that targets the exact posterior over the unknown-function parameters.\n\nReferences\n\nAndrieu, C. & Roberts, G.O. (2009), \"The pseudo-marginal approach for efficient Monte Carlo computations\", Ann. Statist. 37(2), 697–725.\nChkrebtii, O. et al. (2016), \"Bayesian solution uncertainty quantification for differential equations\", Bayesian Analysis 11(4), 1239–1267.\n\nReturns\n\nPSMSolution; convergence holds the MCMC chain (MCMCChains.Chains).\n\nMasked data\n\nThis solver does NOT support masked observations and raises an error if any data cell is masked (data_weights == 0 or a non-finite data_values entry). Its likelihood is evaluated inside a Kalman / particle recursion with no per-cell mask: honouring a mask means skipping the FILTER UPDATE, not merely the density term, for the masked cells. Left unguarded the masked cells corrupt the filter state while the run still looks like an ordinary converged fit, so it fails loudly instead. Drop the masked rows from data_times/data_values, or use one of the masking-capable solvers (LAML, GCVSolver, CollocationLAML, GradientMatching, TwoStageSolver, BNGSolver, ODINSolver, RKHSSolver, IntegralMatchingSolver, AdamSolver, MultipleShootingSolver, DerivativeFreeSolver, MCMCSolver, MagiSolver, VariationalSolver, ABCSolver, ProfileLikelihoodSolver).\n\n\n\n\n\nSciMLBase.solve(prob::PSMProblem, alg::GCVSolver)\n\nFit a partially specified model using IRLS with GCV smoothing parameter selection.\n\nAlgorithm\n\nFor each IRLS iteration:\n\nEvaluate model and compute finite-difference Jacobian\nForm pseudodata z = y − f + J·β\nCompute IRLS weights from current predictions\nSelect λ by minimizing GCV(λ) via grid search + golden-section refinement\nSolve penalized LS at optimal λ\nStep contraction (backtracking)\nRepeat until convergence\n\nReturns a PSMSolution. sol.convergence is a NamedTuple (converged, iterations, reason, gcv) — see the GCVSolver and PSMSolution docstrings for the key taxonomy.\n\n\n\n\n\nsolve(prob::PSMProblem, alg::TwoStageSolver)\n\nFit a partially specified model using a two-stage estimation procedure. In stage 1 the data are smoothed to estimate state trajectories and derivatives; in stage 2 the unknown-function parameters are estimated by nonlinear least squares against those smoothed derivatives.\n\nAlgorithm\n\nSmooth each observed state with a penalized (GCV) smoothing spline and evaluate derivatives at the data time points.\nHold unobserved states constant at their initial-condition values and MASK them out of the matching loss (their fabricated targets carry no information).\nMinimise ∑ₜ ‖x′(t) − f(x(t), uf(t; β))‖² plus the smoothing penalty with respect to β using Adam with ForwardDiff gradients.\nReconstruct the unknown functions at the fitted parameters.\n\nReferences\n\nVoss & Feng (2008), \"Modelling Non-linear Differential Equations: a Two-Stage Method\", JRSS-C.\n\nReturns\n\nPSMSolution with fitted parameters, trajectory, and unknown functions. sol.convergence is a NamedTuple (converged, iterations, reason, method) — see the TwoStageSolver docstring for the key taxonomy.\n\n\n\n\n\nSciMLBase.solve(prob::PSMProblem, alg::DerivativeFreeSolver)\n\nFit a partially specified model using derivative-free optimization.\n\nAlgorithm\n\nDefine a loss function that simulates the model and computes weighted SSE (:mse) or the negative log-likelihood of prob.likelihood (:likelihood). The default loss=:auto picks :mse for Gaussian data and :likelihood for any other family, so a declared non-Gaussian likelihood is honored rather than silently ignored.\nAdd the smoothing penalty 0.5 · penalty_weight · Σ_j β_j' S_j β_j (default penalty_weight=1.0; set 0 for an unpenalized fit).\nUse Optim.NelderMead() or Optim.ParticleSwarm() to minimize the loss.\nReturn a PSMSolution with fitted parameters.\n\nSimulation failures (e.g. ODE divergence) are handled gracefully by returning a large loss value, guiding the optimizer away from bad regions.\n\n\n\n\n\nsolve(prob::PSMProblem, alg::VariationalSolver)\n\nFit a partially specified model using mean-field variational inference. The posterior over unknown-function parameters is approximated by a diagonal Gaussian, optimised by maximising the evidence lower bound (ELBO).\n\nThe ELBO's data term follows prob.likelihood: Gaussian data use the σ²-scaled quadratic form with obs_noise_var (user-supplied or estimated); Poisson, NegativeBinomial, TruncatedNormal, and CustomLikelihood use the family's pointwise log-likelihood with data_weights multiplied in and no Gaussian noise nuisance. Passing obs_noise_var with a non-Gaussian family errors.\n\nAlgorithm\n\nInitialise the variational mean μ from the model's initial parameters and set log-σ to a small value.\nAt each iteration draw n_samples reparametrised samples from q(β) = N(μ, diag(σ²)).\nEstimate the ELBO gradient via the reparametrisation trick and update (μ, log σ) with Adam.\nReturn the posterior mean as point estimate and the variational parameters in sol.extras.\n\nReferences\n\nBlei, Kucukelbir & McAuliffe (2017), \"Variational Inference: A Review for Statisticians\", JASA.\n\nReturns\n\nPSMSolution with fitted parameters, trajectory, unknown functions, and variational parameters μ, σ in sol.extras.\n\n\n\n\n\nsolve(prob::PSMProblem, alg::ABCSolver)\n\nFit a partially specified model using Approximate Bayesian Computation with Sequential Monte Carlo (ABC-SMC). A likelihood-free method that accepts parameter proposals when simulated data are sufficiently close to observed data under a summary statistic.\n\nAlgorithm\n\nDraw n_particles parameter vectors from the prior (uniform around the initial guess).\nFor each ABC-SMC generation, propagate particles through a Gaussian kernel, simulate the model, and compute the summary statistic distance.\nAccept particles within a shrinking tolerance schedule.\nReturn posterior-mean point estimates and the particle ensemble.\n\nReferences\n\nToni et al. (2009), \"Approximate Bayesian computation scheme for parameter inference and model selection\", JRSS-B.\n\nReturns\n\nPSMSolution with fitted parameters, trajectory, unknown functions, and the particle ensemble in sol.extras[:particles].\n\n\n\n\n\nsolve(prob::PSMProblem, alg::IntegralMatchingSolver)\n\nFit a partially specified model by integral matching (Dattner & Klaassen 2015).\n\nInstead of matching noisy derivatives, this method integrates both sides of the ODE, comparing the smoothed trajectory increments with numerical quadrature of the right-hand side. This avoids both derivative estimation and repeated ODE integration, providing a robust and computationally efficient estimator.\n\nAlgorithm\n\nSmooth each observed state with cubic splines → ŷ(t).\nCompute trajectory increments: Δᵢₖ = ŷₖ(tᵢ) − ŷₖ(t₁).\nCompute cumulative integrals of the ODE RHS using the trapezoidal rule evaluated at the smoothed states: Iᵢₖ = ∫₁ⁱ fₖ(ŷ(s), p(β), s) ds.\nMinimize L(β) = Σₖ Σᵢ (Δᵢₖ − Iᵢₖ)² + λ β'Sβ w.r.t. β using Adam.\n\nReturns\n\nPSMSolution with fitted parameters, trajectory, and unknown functions. sol.convergence is a NamedTuple (converged, iterations, reason, method) — see the IntegralMatchingSolver docstring for the key taxonomy.\n\n\n\n\n\nsolve(prob::PSMProblem, alg::ProfileLikelihoodSolver)\n\nCompute profile likelihoods for unknown-function parameters.\n\nFor each profiled parameter index j, sweeps βⱼ over a grid centred on the best-fit value while re-optimising β₋ⱼ at each grid point.  The profile likelihood curve is used to compute likelihood-ratio confidence intervals at the specified level.\n\nFirst runs a full LAML fit to obtain the MLE, then profiles each requested parameter.\n\nReturns\n\nPSMSolution where convergence contains :profiles (a Dict mapping parameter index → NamedTuple of grid values, profile likelihoods, and CI).\n\n\n\n\n\nsolve(prob::PSMProblem, alg::EnsembleKalmanSolver)\n\nFit a partially specified model using Ensemble Kalman Inversion (EKI).\n\nUses an ensemble of parameter particles that are iteratively updated via the Kalman gain to match observations.  The method is derivative-free and naturally handles non-smooth or stiff forward models.\n\nAlgorithm\n\nInitialise J ensemble members θ⁽ʲ⁾ from a prior (centred on initial params).\nFor each iteration n: a. Evaluate forward model G(θ⁽ʲ⁾) for each particle. b. Compute ensemble covariances: Cθg, Cgg. c. Update: θ⁽ʲ⁾ₙ₊₁ = θ⁽ʲ⁾ₙ + Cθg (Cgg + Γ)⁻¹ (y + ξ⁽ʲ⁾ − G(θ⁽ʲ⁾ₙ))\nReturn ensemble mean as the point estimate.\n\nReturns\n\nPSMSolution with fitted parameters and convergence containing :ensemble_spread (final ensemble std) and :ensemble_history.\n\nsol.convergence.converged is honest: it is true only when the ensemble actually collapsed (mean particle std fell to 1e-3 of its initial value, reason = :ensemble_collapse). Exhausting n_iterations reports converged = false, reason = :maxiters, and a failed forward solve at the ensemble mean — which makes the fitted values NaN and data_loss Inf — reports reason = :final_solve_failed.\n\nNote the discrete-time convention: discrete problems are propagated with simulate_discrete (unit steps over tspan, data times snapped to the nearest integer step), the same trajectory every other solver sees.\n\nMasked data\n\nThis solver does NOT support masked observations and raises an error if any data cell is masked (data_weights == 0 or a non-finite data_values entry). Its likelihood is evaluated inside a Kalman / particle recursion with no per-cell mask: honouring a mask means skipping the FILTER UPDATE, not merely the density term, for the masked cells. Left unguarded the masked cells corrupt the filter state while the run still looks like an ordinary converged fit, so it fails loudly instead. Drop the masked rows from data_times/data_values, or use one of the masking-capable solvers (LAML, GCVSolver, CollocationLAML, GradientMatching, TwoStageSolver, BNGSolver, ODINSolver, RKHSSolver, IntegralMatchingSolver, AdamSolver, MultipleShootingSolver, DerivativeFreeSolver, MCMCSolver, MagiSolver, VariationalSolver, ABCSolver, ProfileLikelihoodSolver).\n\n\n\n\n\nsolve(prob::PSMProblem, alg::ODINSolver)\n\nFit a partially specified model using ODE-Informed regression (ODIN; Wenk & Abbati et al. 2020).\n\nStage 1 — per observed state, GP hyperparameters (σ², ℓ, σ_n²) are estimated by maximising the GP marginal likelihood of the (centered) data (or taken from the solver if supplied). From these, three fixed matrices are built at the data times: the prior precision K⁻¹, the derivative map D = 'K K⁻¹ (states ↦ GP conditional-mean derivative), and the conditional derivative covariance A = ''K − 'K K⁻¹ 'Kᵀ + γI.\n\nStage 2 — the states X (all of them, including unobserved) and the unknown-function parameters θ are optimised jointly by Adam on\n\nR(X, θ) = Σ_k [ ‖y_k − x_k‖²/σ_{n,k}²      (observed states only)\n                + x̃_kᵀ K_k⁻¹ x̃_k            (GP prior, x̃ centered)\n                + (f_k(X,θ) − D_k x̃_k)ᵀ A_k⁻¹ (f_k(X,θ) − D_k x̃_k) ]\n                + θ smoothing penalty,\n\nso the ODE mismatch is weighted by how well the GP determines the derivative (tight where data are dense, loose where sparse), and states may move away from the GP posterior mean when the ODE demands it. Unobserved states carry a GP prior with hyperparameters borrowed from the observed states (mean lengthscale/variance), centered at their initial condition, and are identified through the ODE terms alone.\n\nReturns\n\nPSMSolution with fitted parameters, the jointly optimised trajectory, and unknown functions. sol.convergence.gp_hyperparams records the per-state (σ², ℓ, σ_n²); sol.convergence also carries the honest convergence keys (converged, iterations, reason) — see the ODINSolver docstring.\n\n\n\n\n\nsolve(prob::PSMProblem, alg::RKHSSolver)\n\nFit a partially specified model with the state trajectory represented in a time-kernel RKHS (González et al. 2014).\n\nEach state is expanded over the data times, x_k(t) = m_k + Σᵢ b_{k,i} k(t, tᵢ), giving the analytic derivative ẋ_k(t) = Σᵢ b_{k,i} k̇(t, tᵢ). The objective\n\nJ(B, θ) = Σ_k [ ‖y_k − x_k(t_data)‖² + λ b_kᵀ K b_k ]\n          + ρ Σ_k Σ_g (ẋ_k(t_g) − f_k(x(t_g), θ))² + θᵀSθ\n\n(data fit + RKHS norm + ODE-gradient match on a collocation grid + approximator smoothing penalty) is minimized by alternating:\n\nB-step — one Gauss–Newton step on ALL states' coefficients jointly: f is linearized around the previous trajectory using the grid Jacobian ∂f/∂x, making every equation's mismatch linear in every state's coefficients, and the joint normal equations (size n_times · n_vars) are solved directly. The Jacobian coupling is what identifies unobserved states.\nθ-step — Adam on the ODE-gradient mismatch with the trajectory fixed.\n\nUnobserved states have no data term (w_k = 0) and are identified through the ODE term alone. All approximator types are supported.\n\nReturns\n\nPSMSolution: params are the approximator coefficients θ, the fitted values are the RKHS trajectory at the data times.\n\n\n\n\n\n","title":"CommonSolve.solve"},{"category":"function","location":"api/#PartiallySpecifiedModels.simulate","page":"API Reference","text":"simulate(prob, beta)\n\nSimulate the model with parameter vector β. Returns predicted values at data times as matrix (ntimes × nobs). Dispatches to ODE integration (continuous) or explicit iteration (discrete).\n\n\n\n\n\n","title":"PartiallySpecifiedModels.simulate"},{"category":"function","location":"api/#PartiallySpecifiedModels.predict","page":"API Reference","text":"predict(sol::PSMSolution, prob::PSMProblem)\n\nPredict at data times using the fitted solution.\n\n\n\n\n\n","title":"PartiallySpecifiedModels.predict"},{"category":"function","location":"api/#PartiallySpecifiedModels.nparams","page":"API Reference","text":"nparams(approx::AbstractApproximator) -> Int\n\nReturn the number of free parameters for the given approximator.\n\n\n\n\n\n","title":"PartiallySpecifiedModels.nparams"},{"category":"function","location":"api/#PartiallySpecifiedModels.initial_params","page":"API Reference","text":"initial_params(approx::AbstractApproximator) -> Vector{Float64}\n\nReturn a vector of initial parameter values for the given approximator.\n\n\n\n\n\n","title":"PartiallySpecifiedModels.initial_params"},{"category":"function","location":"api/#PartiallySpecifiedModels.penalty_matrix","page":"API Reference","text":"penalty_matrix(a::BSplineApproximator)\n\nReturn the smoothing penalty matrix for a B-spline approximator. Computed on the unit interval for scale-invariant smoothing parameters.\n\n\n\n\n\npenalty_matrix(a::NeuralApproximator)\n\nReturns scaled identity matrix for L2 regularization when penalty_weight > 0, enabling LAML smoothing parameter estimation. Returns nothing otherwise.\n\n\n\n\n\npenalty_matrix(a::GPApproximator)\n\nReturns a second-derivative (spline-style) penalty matrix on the inducing points.\n\nWhile the theoretical GP penalty is f'K⁻¹f (negative log-prior), the K⁻¹ matrix has a narrow eigenvalue spectrum that makes LAML smoothing parameter estimation unreliable — the penalty lacks the dynamic range to distinguish smooth from wiggly solutions. Using a spline penalty ∫(f'')²dx instead gives LAML a well-conditioned penalty with wide eigenvalue range, while the GP kernel is still used for evaluation (interpolation between inducing points).\n\n\n\n\n\npenalty_matrix(a::SPDEApproximator)\n\nReturn the Matérn SPDE penalty matrix.\n\n\n\n\n\npenalty_matrix(a::ShapeConstrainedSPDEApproximator)\n\nPenalty matrix in the unconstrained parameter space (γ). Two modes, selected by the approximator's penalty field:\n\n:gamma_matern (default): the Matérn FEM precision P_β is pushed through the LINEAR map Σ as P_γ = Σᵀ P_β Σ, ignoring the nonlinear softplus in β = Σ·d(γ). Since P_β is SPD and Σ has full column rank, P_γ is SPD — it has NO null space. Consequently λ→∞ drives γ → 0 entirely, whose image under the constraint map is a fixed ramp with per-node increment softplus(0) = log 2 (for monotone constraints) — an arbitrary function, not a maximally smooth member of the constraint family — and the free level γ₁ is shrunk along with everything else. Kept as the default for backward compatibility.\n:difference: the Pya & Wood (2015) SCOP first-difference penalty DᵀD built directly on γ, mirroring the penalty_matrix(::ShapeConstrainedBSplineApproximator) construction: the chain of first differences runs over the curvature-carrying components only, skipping the free level (and the slope-like component for curvature constraints) per _penalty_skip_indices. The null space then contains the free level/slope shifts and the constant-increment vector, so the λ→∞ limit is a maximally smooth member of the constraint family (e.g. the best-fitting straight line for :increasing), with the level unshrunk. The SCSPDE mesh is always uniform (built with range), so the plain unweighted difference matrix is the natural choice — no mesh weighting is needed. Note this mode does not use the Matérn κ/ρ structure; smoothness is controlled by λ alone.\n\nPrefer :difference when you want the classical penalized-spline λ→∞ semantics (shrinkage toward the smooth null family); keep :gamma_matern if you want Matérn-correlated shrinkage of γ toward zero and accept its fixed-ramp limit.\n\n\n\n\n\npenalty_matrix(a::COMONetApproximator)\n\nL2 (Tikhonov) penalty matrix for COMONet: penalty_weight * I. This provides gentle regularization on the unconstrained weights.\n\n\n\n\n\n","title":"PartiallySpecifiedModels.penalty_matrix"},{"category":"function","location":"api/#PartiallySpecifiedModels.spline_penalty_matrix","page":"API Reference","text":"spline_penalty_matrix(knots_x)\n\nCompute the natural cubic spline smoothing penalty matrix S such that the wiggliness penalty is y'Sy = ∫(f'')² dx.\n\nThis is the standard result: S = H'B⁻¹H where H contains second divided differences and B is the tridiagonal mass matrix from cubic spline theory. The matrix has rank nknots - 2.\n\nReference: Green & Silverman (1994), Chapter 2.\n\n\n\n\n\n","title":"PartiallySpecifiedModels.spline_penalty_matrix"},{"category":"function","location":"api/#PartiallySpecifiedModels.optimize_spde_range","page":"API Reference","text":"optimize_spde_range(prob, alg; kwargs...) -> NamedTuple\n\nOptimize the SPDE range parameter via profile GCV.\n\nRuns the solver for each candidate range parameter on a log-scale grid, and returns the solution with the best GCV score. All SPDE approximators in the problem share the same range multiplier.\n\nKeyword Arguments\n\nrange_multipliers::AbstractVector: explicit multipliers of the default range to try (overrides n_grid).\nn_grid::Int=10: number of log-spaced grid points from 0.1× to 10× the default range.\nverbose::Bool=false: print progress for each grid point.\n\nReturns\n\nA NamedTuple with fields:\n\nsolution::PSMSolution — best solution found\nrange_param::Float64 — optimal range parameter (of the first SPDE approximator)\ngcv_scores::Vector{Float64} — GCV score at each grid point (Inf if failed)\nrange_values::Vector{Float64} — range parameter values tried\n\n\n\n\n\n","title":"PartiallySpecifiedModels.optimize_spde_range"},{"category":"function","location":"api/#PartiallySpecifiedModels.with_range_param","page":"API Reference","text":"with_range_param(approx, range_param)\n\nCreate a copy of an SPDE approximator with a new range parameter. Non-SPDE approximators are returned unchanged.\n\n\n\n\n\n","title":"PartiallySpecifiedModels.with_range_param"},{"category":"function","location":"api/#PartiallySpecifiedModels.bootstrap","page":"API Reference","text":"bootstrap(sol, prob, alg; nboot=200, method=:parametric, level=0.95,\n          uf_ngrid=100, rng=default_rng(), verbose=false)\n\nCompute bootstrap confidence intervals for a PSM solution.\n\nArguments\n\nsol::PSMSolution: the original fitted solution\nprob::PSMProblem: the problem definition\nalg: the solver algorithm (e.g., LAML(...))\nnboot::Int=200: number of bootstrap replicates\nmethod::Symbol=:parametric: bootstrap method\n:parametric — sample from the fitted distribution (Gaussian, Poisson, NegBin, TruncatedNormal — uses the problem's likelihood family)\n:nonparametric — resample residuals with replacement per state (Gaussian likelihoods only: additive residuals are invalid pseudo-data for count or truncated families)\n\nEach replicate is refit from scratch with alg, so smoothing parameters are re-estimated per replicate; the intervals therefore include smoothing- selection variability (unlike a fixed-λ conditional bootstrap).\n\nlevel::Float64=0.95: confidence level for CIs\nuf_ngrid::Int=100: number of grid points for unknown function CIs\nrng: random number generator\nparallel::Bool=false: use multi-threading (Threads.@threads) for replicates. Requires Julia started with JULIA_NUM_THREADS > 1.\nverbose::Bool=false: print progress\n\nReturns\n\nA BootstrapResult with coefficient samples, fitted value CIs, and unknown function CIs.\n\nExample\n\nsol = solve(prob, LAML(maxiters=80))\nbs = bootstrap(sol, prob, LAML(maxiters=80); nboot=200, verbose=true)\n\n# Plot fitted values with 95% CI ribbon\nplot(sol.data_times, sol.fitted_values[:, 1], lw=2)\nplot!(sol.data_times, bs.ci_fitted.lower[:, 1], fillrange=bs.ci_fitted.upper[:, 1],\n      alpha=0.2, label=\"95% CI\")\n\n# Plot unknown function with CI\nplot(bs.uf_grid[:λ], bs.ci_uf[:λ].lower, fillrange=bs.ci_uf[:λ].upper,\n     alpha=0.2, label=\"95% CI\")\n\n\n\n\n\n","title":"PartiallySpecifiedModels.bootstrap"},{"category":"type","location":"api/#PartiallySpecifiedModels.BootstrapResult","page":"API Reference","text":"BootstrapResult\n\nResult of bootstrap resampling for a PSM solution.\n\nFields\n\ncoefs::Matrix{Float64}: B × p matrix of bootstrap coefficient vectors\nfitted_values::Array{Float64,3}: ntimes × nobs × B array of fitted trajectories\nuf_values::Dict{Symbol, Matrix{Float64}}: unknown function evaluations on a grid,  each n_grid × B matrix\nuf_grid::Dict{Symbol, Vector{Float64}}: evaluation grid for each UF\nci_fitted::NamedTuple: (lower, upper) matrices (ntimes × nobs) at given level\nci_uf::Dict{Symbol, NamedTuple}: (lower, upper) vectors for each UF\nlevel::Float64: confidence level (e.g., 0.95)\nn_success::Int: number of successful bootstrap replicates (out of B attempted)\n\n\n\n\n\n","title":"PartiallySpecifiedModels.BootstrapResult"},{"category":"function","location":"api/#PartiallySpecifiedModels.confidence_band","page":"API Reference","text":"confidence_band(sol::PSMSolution, prob::PSMProblem;\n                level=0.95, uf_ngrid=100)\n\nCompute Bayesian confidence/credible bands for the unknown functions using the posterior covariance from the LAML fit.\n\nThe posterior covariance is V_β = σ̂² (J'WJ + S^λ)⁻¹, and the pointwise standard error of f(x) is se(x) = √(b(x)' V_β b(x)), where b(x) is the basis vector mapping coefficients to function value.\n\nThese \"across-the-function\" intervals (Nychka 1988, Wood 2006 §4.8) account for smoothing bias and typically achieve near-nominal coverage, unlike bootstrap CIs which only capture sampling variability.\n\nReturns a Dict mapping each unknown function name to a NamedTuple (grid, fitted, lower, upper, se).\n\nExample\n\nsol = solve(prob, LAML(maxiters=80))\nbands = confidence_band(sol, prob)\n\n# Plot unknown function with 95% credible band\nplot(bands[:λ].grid, bands[:λ].fitted, lw=2, label=\"Estimated λ\")\nplot!(bands[:λ].grid, bands[:λ].lower,\n      fillrange=bands[:λ].upper, fillalpha=0.2, label=\"95% CI\")\n\n\n\n\n\n","title":"PartiallySpecifiedModels.confidence_band"},{"category":"function","location":"api/#PartiallySpecifiedModels.appraise","page":"API Reference","text":"appraise(sol::PSMSolution; family=nothing)\n\nCompute diagnostic data for a standard 4-panel goodness-of-fit display, following the pattern of R's gratia::appraise() for GAMs.\n\nReturns a named tuple with fields:\n\nresiduals: standardized residuals (deviance if family given, else response/σ̂)\nfitted: fitted values (vectorized across all observed states)\nobserved: observed values (vectorized)\nqq_theoretical: theoretical normal quantiles\nqq_sample: sorted standardized residuals\ndurbin_watson: DW statistic per observed state\nusable: the cell mask actually used (see _diag_usable)\n\nMasked observations (a NaN/non-finite stored value) are dropped BEFORE every statistic and every denominator, so residuals, fitted, observed and the QQ vectors have length equal to the number of usable cells rather than length(sol.data_values). Without this a single masked cell made the std — and hence every standardized residual — NaN. See _diag_usable for the one masking case that cannot be detected from a PSMSolution.\n\nExample\n\ndiag = appraise(sol)\n\n# 4-panel plot with Plots.jl:\np_qq = scatter(diag.qq_theoretical, diag.qq_sample, title=\"QQ Plot\")\np_rf = scatter(diag.fitted, diag.residuals, title=\"Residuals vs Fitted\")\np_hist = histogram(diag.residuals, title=\"Histogram of Residuals\")\np_of = scatter(diag.observed, diag.fitted, title=\"Observed vs Fitted\")\nplot(p_qq, p_rf, p_hist, p_of, layout=(2,2))\n\n\n\n\n\n","title":"PartiallySpecifiedModels.appraise"},{"category":"function","location":"api/#PartiallySpecifiedModels.deviance_residuals","page":"API Reference","text":"deviance_residuals(family, y, mu)\n\nCompute per-observation deviance residuals: r_i^D = textsign(y_i - mu_i) sqrtd_i where d_i is the unit deviance contribution.\n\nFor a well-specified model, deviance residuals are approximately standard normal.\n\n\n\n\n\n","title":"PartiallySpecifiedModels.deviance_residuals"},{"category":"function","location":"api/#PartiallySpecifiedModels.residual_diagnostics","page":"API Reference","text":"residual_diagnostics(sol::PSMSolution)\n\nCompute a suite of residual diagnostics for a PSM solution.\n\nReturns a named tuple with:\n\nresiduals: raw residuals (observed - fitted), full (time × state) shape\nusable: cell mask; residuals is non-finite exactly where this is false\ndurbin_watson: DW statistic per observed state (2.0 = no autocorrelation)\nacf: autocorrelation at lags 1:maxlag per state\nsemivariogram: (lags, gamma) per observed state\n\nMasked observations are excluded from every statistic and every denominator: each column's DW, ACF and semivariogram are computed over that column's usable rows only (with the matching data_times subset for the semivariogram lags). residuals keeps its rectangular shape, so a masked cell shows up there as the NaN it is — read it through usable. Before this, one masked cell made durbin_watson, the whole acf and the semivariogram NaN. See _diag_usable for the masking case that cannot be detected from a PSMSolution.\n\n\n\n\n\n","title":"PartiallySpecifiedModels.residual_diagnostics"},{"category":"function","location":"api/#PartiallySpecifiedModels.durbin_watson","page":"API Reference","text":"durbin_watson(residuals)\n\nCompute the Durbin–Watson statistic for temporal autocorrelation in residuals.\n\nDW ≈ 2: no autocorrelation (good fit)\nDW < 2: positive autocorrelation (oversmoothing — systematic patterns remain)\nDW > 2: negative autocorrelation (overfitting — alternating residuals)\n\nFor multivariate data, computes per-column and returns a vector.\n\n\n\n\n\n","title":"PartiallySpecifiedModels.durbin_watson"},{"category":"function","location":"api/#PartiallySpecifiedModels.residual_acf","page":"API Reference","text":"residual_acf(residuals; maxlag=10)\n\nCompute empirical autocorrelation function (ACF) of residuals at lags 1:maxlag.\n\nReturns a vector of autocorrelations. Values significantly different from zero at low lags indicate the model is over- or under-smoothing.\n\n\n\n\n\n","title":"PartiallySpecifiedModels.residual_acf"},{"category":"function","location":"api/#PartiallySpecifiedModels.semivariogram","page":"API Reference","text":"semivariogram(times, residuals; maxlag=nothing, nbins=15)\n\nCompute empirical semivariogram γ(h) = (1/2|N(h)|) Σ (r(t) - r(t+h))².\n\nReturns (lag_centers, gamma). For a well-fitted model with independent residuals, γ(h) should be approximately constant (≈ σ²) across all lags. A rising semivariogram at small lags indicates positive autocorrelation (oversmoothing).\n\n\n\n\n\n","title":"PartiallySpecifiedModels.semivariogram"},{"category":"section","location":"getting_started/#Getting-Started","page":"Getting Started","text":"This tutorial introduces the basic workflow of PartiallySpecifiedModels.jl using a simple example: recovering an unknown per-capita growth rate from population data.","title":"Getting Started"},{"category":"section","location":"getting_started/#Overview","page":"Getting Started","text":"Partially specified models (PSMs) are dynamical systems where one or more functional responses are left unspecified and estimated from data using flexible approximators such as penalized B-splines. This approach is particularly useful in ecology, where the form of density-dependent processes is often unknown.\n\nThe basic workflow is:\n\nDefine an ODE model with unknown functions\nChoose approximators for the unknowns (B-splines, neural networks, etc.)\nBuild a PSMProblem with data\nSolve with a fitting algorithm (e.g., LAML)\nInspect the fitted solution","title":"Overview"},{"category":"section","location":"getting_started/#Setup","page":"Getting Started","text":"using PartiallySpecifiedModels\nusing PartiallySpecifiedModels: solve\nusing OrdinaryDiffEq\nusing Random\nRandom.seed!(42)\nnothing # hide","title":"Setup"},{"category":"section","location":"getting_started/#A-Simple-Example:-Logistic-Growth","page":"Getting Started","text":"Consider a population N(t) growing according to\n\nfracdNdt = r(N) cdot N\n\nwhere r(N) is the per-capita growth rate as a function of population size. In standard exponential growth, r is constant. In logistic growth, r(N) = r_0(1 - NK). In a PSM, we leave r(N) unspecified and estimate it from data.","title":"A Simple Example: Logistic Growth"},{"category":"section","location":"getting_started/#Generate-synthetic-data","page":"Getting Started","text":"We generate data from a logistic model with r_0 = 05 and K = 10, observed with Gaussian noise:\n\nr0, K, N0 = 0.5, 10.0, 0.5\ntspan = (0.0, 15.0)\ndata_times = collect(0.0:0.5:15.0)\n\nfunction logistic!(du, u, p, t)\n    du[1] = r0 * (1 - u[1] / K) * u[1]\nend\ntrue_prob = ODEProblem(logistic!, [N0], tspan)\ntrue_sol = OrdinaryDiffEq.solve(true_prob, Tsit5(); saveat=data_times)\ntrue_N = [true_sol(t)[1] for t in data_times]\n\nσ_noise = 0.3\nobserved_N = true_N .+ σ_noise .* randn(length(data_times))\nobserved_N = max.(observed_N, 0.01)  # ensure positive\nnothing # hide","title":"Generate synthetic data"},{"category":"section","location":"getting_started/#Define-the-PSM","page":"Getting Started","text":"The ODE function receives a parameter struct p containing callable unknown functions and any known parameters. Here, p.r is the unknown growth rate function:\n\nfunction growth!(du, u, p, t)\n    N = u[1]\n    du[1] = p.r(N) * N\nend\nnothing # hide","title":"Define the PSM"},{"category":"section","location":"getting_started/#Choose-an-approximator","page":"Getting Started","text":"We model r(N) with a penalized cubic B-spline having 8 evenly-spaced knots over the range of population sizes:\n\napprox_r = BSplineApproximator(:r, (0.0, 12.0), 8;\n                                initial = x -> 0.3)\n\nThis creates a spline approximator with:\n\nName: :r — how it appears in the parameter struct\nDomain: 0 12 — the range of N values\n8 knots — degrees of freedom for the shape of r(N)\nInitial value: constant r = 03 everywhere","title":"Choose an approximator"},{"category":"section","location":"getting_started/#Build-the-problem","page":"Getting Started","text":"prob = PSMProblem(\n    growth!,                        # ODE dynamics\n    [N0],                           # initial conditions\n    tspan,                          # time span\n    [approx_r];                     # unknown function approximators\n    data_times = data_times,\n    data_values = reshape(observed_N, :, 1),  # n_times × 1 matrix\n    obs_to_state = [1],             # observe state variable 1\n    likelihood = Gaussian(),        # Gaussian errors\n    solver = Tsit5()                # ODE solver\n)\nnothing # hide","title":"Build the problem"},{"category":"section","location":"getting_started/#Solve-with-LAML","page":"Getting Started","text":"The LAML algorithm estimates the spline coefficients and the smoothing parameter lambda simultaneously. For Gaussian data, LAML is equivalent to Restricted Maximum Likelihood (REML).\n\nsol = solve(prob, LAML())\nnothing # hide","title":"Solve with LAML"},{"category":"section","location":"getting_started/#Inspect-the-solution","page":"Getting Started","text":"The solution contains fitted values, estimated unknown functions, and diagnostics:\n\nprintln(\"Data loss: \", round(sol.data_loss, digits=4))\nprintln(\"EDF: \", round(sol.edf, digits=2))\nprintln(\"Smoothing params: \", round.(sol.smoothing_params, digits=4))\n\nr_fitted = sol.unknown_functions[:r]\n\nN_grid = [1.0, 3.0, 5.0, 7.0, 9.0]\nfor N in N_grid\n    r_est = round(r_fitted(N), digits=3)\n    r_true = round(r0 * (1 - N / K), digits=3)\n    println(\"r($N): estimated=$r_est, true=$r_true\")\nend\n\nThe fitted r(N) should resemble the true logistic form r(N) = r_0(1 - NK) — an approximately linear decline — without assuming any parametric form.","title":"Inspect the solution"},{"category":"section","location":"getting_started/#Key-Concepts","page":"Getting Started","text":"","title":"Key Concepts"},{"category":"section","location":"getting_started/#The-PSMProblem","page":"Getting Started","text":"A PSMProblem combines:\n\nComponent Description\ndynamics! ODE right-hand side f!(du, u, p, t)\nu0 Initial conditions (vector or function of p)\ntspan Time interval (t₀, t₁)\napproximators Vector of AbstractApproximator (splines, neural nets)\ndata_times Observation times\ndata_values Data matrix (n_times × n_obs)\nlikelihood Error distribution (Gaussian, Poisson, etc.)\nsolver ODE solver from OrdinaryDiffEq.jl","title":"The PSMProblem"},{"category":"section","location":"getting_started/#Missing-observations","page":"Getting Started","text":"Incomplete data does not need reshaping. Mark a missing cell in data_values as NaN and give it weight 0.0 in data_weights:\n\ndata_values[3, 1]  = NaN\ndata_weights[3, 1] = 0.0\n\nSuch a masked cell is dropped from the objective, the reported loss, the scale estimate, every denominator, and the residual diagnostics. Marking it both ways matters: 0 * NaN = NaN in IEEE arithmetic, so a zero weight on its own does not neutralise a NaN value.\n\nMost solvers support masking. Four — RodeoSolver, DaltonSolver, PseudoMarginalSolver and EnsembleKalmanSolver — evaluate their likelihood inside a Kalman/particle recursion with no per-cell mask and raise an error instead of silently corrupting the filter. See Missing and Masked Observations for the full list and the reasoning.","title":"Missing observations"},{"category":"section","location":"getting_started/#Smoothing-and-EDF","page":"Getting Started","text":"The effective degrees of freedom (EDF) measures model complexity. With 8 knots, the maximum EDF is 8 (unpenalized). LAML estimates lambda to balance fit and smoothness:\n\nSmall lambda: less smoothing, higher EDF, more flexible\nLarge lambda: more smoothing, lower EDF, smoother curves\n\nFor the logistic growth example, the EDF should be close to 2 (since the true r(N) is linear).","title":"Smoothing and EDF"},{"category":"section","location":"getting_started/#Summary","page":"Getting Started","text":"The basic workflow is:\n\nWrite dynamics!(du, u, p, t) — access unknown functions via p.name(x)\nCreate BSplineApproximator(:name, domain, nknots) for each unknown\nBuild PSMProblem(dynamics!, u0, tspan, approximators; data=..., likelihood=...)\nCall solve(prob, LAML())\nAccess sol.unknown_functions[:name] for the fitted functions\n\nSee the Approximators and Solvers pages for detailed documentation of all available options, or the Vignettes page for 35 worked examples.","title":"Summary"},{"category":"section","location":"solvers/#Solvers","page":"Solvers","text":"PartiallySpecifiedModels.jl provides 22 solvers for fitting partially specified models. Solvers are passed as the second argument to solve:\n\nusing PartiallySpecifiedModels # hide\nusing PartiallySpecifiedModels: solve # hide\nusing OrdinaryDiffEq, Random # hide\nRandom.seed!(42) # hide\nfunction growth!(du, u, p, t) # hide\n    du[1] = p.r(u[1]) * u[1] # hide\nend # hide\napprox_r = BSplineApproximator(:r, (0.0, 12.0), 8; initial = x -> 0.3) # hide\nr0, K, N0 = 0.5, 10.0, 0.5 # hide\ntrue_sol = OrdinaryDiffEq.solve(ODEProblem((du,u,p,t) -> du[1] = r0*(1-u[1]/K)*u[1], [N0], (0.0,15.0)), Tsit5(); saveat=0.5) # hide\nobs = max.([true_sol.u[i][1] + 0.3*randn() for i in 1:length(true_sol.t)], 0.01) # hide\nprob = PSMProblem(growth!, [N0], (0.0,15.0), [approx_r]; data_times=collect(true_sol.t), data_values=reshape(obs,:,1), obs_to_state=[1], likelihood=Gaussian(), solver=Tsit5()) # hide\nsol = solve(prob, LAML())\nprintln(\"Data loss: \", round(sol.data_loss, digits=4), \", EDF: \", round(sol.edf, digits=2))","title":"Solvers"},{"category":"section","location":"solvers/#Penalized-Likelihood-Solvers","page":"Solvers","text":"","title":"Penalized Likelihood Solvers"},{"category":"section","location":"solvers/#LAML","page":"Solvers","text":"Penalized Iteratively Reweighted Least Squares (P-IRLS) with Laplace Approximate Marginal Likelihood for automatic smoothing parameter selection. The default and recommended solver for B-spline approximators. For Gaussian data, LAML is equivalent to REML.","title":"LAML"},{"category":"section","location":"solvers/#GCVSolver","page":"Solvers","text":"Penalized IRLS with Generalized Cross-Validation for smoothing parameter selection. An alternative to LAML that minimizes leave-one-out prediction error.","title":"GCVSolver"},{"category":"section","location":"solvers/#CollocationLAML","page":"Solvers","text":"Generalized profiling (collocation) approach. Fits spline approximations to the state variables first, then optimizes the unknown function parameters to match the implied derivatives. Can be more robust than direct ODE fitting for stiff or chaotic systems.","title":"CollocationLAML"},{"category":"section","location":"solvers/#Gradient-Matching-Solvers","page":"Solvers","text":"These solvers avoid numerical ODE integration entirely by matching derivatives of smoothed data to the model equations.","title":"Gradient Matching Solvers"},{"category":"section","location":"solvers/#GradientMatching","page":"Solvers","text":"Smooth the observed data, compute numerical derivatives, then fit the unknown functions to match. Requires good data coverage and low noise.","title":"GradientMatching"},{"category":"section","location":"solvers/#TwoStageSolver","page":"Solvers","text":"The simplest gradient matching approach (Varah 1982): smooth the data with splines, differentiate, then regress the unknown functions on the derivatives. A useful baseline for comparison.","title":"TwoStageSolver"},{"category":"section","location":"solvers/#AdaptiveGradientMatching","page":"Solvers","text":"GP-based gradient matching using the product-of-experts formulation of Dondelinger et al. (2013). Default mode is a fast MAP fit; with n_samples > 0 it runs the paper's tempered population MCMC, jointly sampling latent states, parameters, and mismatch variances across a temperature ladder with exchange moves, returning cold-chain posterior draws in convergence.beta_samples.","title":"AdaptiveGradientMatching"},{"category":"section","location":"solvers/#BNGSolver","page":"Solvers","text":"Ensemble Bayesian gradient matching (Bonnaffé & Coulson 2023). Smooth-then-match under a variance-marginalized log-posterior, repeated over k_obs residual-bootstrap resamples × k_proc restarts; unknown functions are posterior-weighted ensemble means with pointwise uncertainty in convergence.ensemble_std. Fast (no ODE integration) and suitable for complex dynamics.","title":"BNGSolver"},{"category":"section","location":"solvers/#Optimization-Based-Solvers","page":"Solvers","text":"","title":"Optimization-Based Solvers"},{"category":"section","location":"solvers/#AdamSolver","page":"Solvers","text":"Gradient-based optimization through the ODE solver using the Adam optimizer. This is the standard approach for Universal Differential Equations (UDEs). Works with all approximator types including neural networks.","title":"AdamSolver"},{"category":"section","location":"solvers/#MultipleShootingSolver","page":"Solvers","text":"Multiple shooting with Adam optimization. Divides the time span into segments and optimizes continuity constraints. More robust than single shooting for long time series or chaotic dynamics.","title":"MultipleShootingSolver"},{"category":"section","location":"solvers/#DerivativeFreeSolver","page":"Solvers","text":"Derivative-free optimization using Nelder-Mead or Particle Swarm methods. A robust fallback when gradient-based methods fail. Slower but more reliable for non-smooth or multimodal objectives.","title":"DerivativeFreeSolver"},{"category":"section","location":"solvers/#Probabilistic-Numerics-Solvers","page":"Solvers","text":"","title":"Probabilistic Numerics Solvers"},{"category":"section","location":"solvers/#RodeoSolver","page":"Solvers","text":"Probabilistic ODE solver based on Kalman filtering/smoothing (Wu & Lysy 2024; the :fenrir likelihood variant follows Tronarp et al. 2022). Provides uncertainty estimates from the numerical integration itself, not just from parameter uncertainty.","title":"RodeoSolver"},{"category":"section","location":"solvers/#DaltonSolver","page":"Solvers","text":"Data-adaptive likelihood with transformed observations using Kalman filtering. Combines probabilistic ODE solving with a likelihood that adapts to the data structure.","title":"DaltonSolver"},{"category":"section","location":"solvers/#Bayesian-Solvers","page":"Solvers","text":"","title":"Bayesian Solvers"},{"category":"section","location":"solvers/#MCMCSolver","page":"Solvers","text":"Full Bayesian inference using Hamiltonian Monte Carlo (HMC) or No-U-Turn Sampler (NUTS). Provides posterior distributions over all parameters including unknown function coefficients.","title":"MCMCSolver"},{"category":"section","location":"solvers/#MagiSolver","page":"Solvers","text":"Manifold-constrained Gaussian Process Inference. Uses GPs to represent both state trajectories and unknown functions, with the ODE constraints enforced on a manifold. Handles partially observed systems well.","title":"MagiSolver"},{"category":"section","location":"solvers/#PseudoMarginalSolver","page":"Solvers","text":"Pseudo-marginal MCMC (Andrieu & Roberts 2009): a Metropolis-Hastings sampler that targets the exact posterior using an unbiased likelihood estimate, here computed with a probabilistic ODE solver so that numerical integration error is accounted for.","title":"PseudoMarginalSolver"},{"category":"section","location":"solvers/#VariationalSolver","page":"Solvers","text":"Mean-field variational inference for fast approximate Bayesian inference. Approximates the posterior with a factorized Gaussian distribution. Much faster than MCMC but may underestimate uncertainty.","title":"VariationalSolver"},{"category":"section","location":"solvers/#ABCSolver","page":"Solvers","text":"Approximate Bayesian Computation with Sequential Monte Carlo (ABC-SMC). Likelihood-free inference that only requires the ability to simulate from the model. Useful when the likelihood is intractable.","title":"ABCSolver"},{"category":"section","location":"solvers/#Additional-Solvers","page":"Solvers","text":"","title":"Additional Solvers"},{"category":"section","location":"solvers/#IntegralMatchingSolver","page":"Solvers","text":"Integral matching (Dattner & Klaassen 2015): integrates both sides of the ODE and matches smoothed trajectory increments against the cumulative integral of the right-hand side. Integration-free and more noise-robust than derivative matching.","title":"IntegralMatchingSolver"},{"category":"section","location":"solvers/#ProfileLikelihoodSolver","page":"Solvers","text":"Profile likelihood for identifiability analysis and confidence intervals (Simpson & Maclaren 2023). Each unknown-function parameter is swept over a grid; the profile is taken through the penalized objective at the fitted smoothing parameters λ̂ — a penalized spline is not identified through its raw RSS — and the statistic ΔPenSS/σ̂² is referenced against χ²₁, with CI endpoints interpolated between grid points. Nuisance coefficients are re-optimised at each grid point by a long Nelder–Mead run. Gaussian likelihoods only.","title":"ProfileLikelihoodSolver"},{"category":"section","location":"solvers/#EnsembleKalmanSolver","page":"Solvers","text":"Ensemble Kalman Inversion (Iglesias et al. 2013): propagates an ensemble of parameter particles through the forward model and updates them with the Kalman gain. Derivative-free batch estimation.","title":"EnsembleKalmanSolver"},{"category":"section","location":"solvers/#ODINSolver","page":"Solvers","text":"ODE-informed regression (ODIN; Wenk et al. 2020): GP hyperparameters are estimated per state by marginal likelihood, then states and unknown-function parameters are optimised jointly under a Mahalanobis risk that weights the ODE mismatch by the GP's conditional derivative covariance. Supports partially observed systems (unobserved states are identified through the ODE terms).","title":"ODINSolver"},{"category":"section","location":"solvers/#RKHSSolver","page":"Solvers","text":"Trajectory-RKHS estimation (González et al. 2014): the state trajectory is represented in a time-kernel RKHS so its derivative is analytic, and fitting alternates a linear Gauss–Newton solve for the trajectory coefficients (data + RKHS norm + linearized ODE-gradient term) with gradient steps on the unknown-function parameters. Supports partially observed systems; no ODE integration.","title":"RKHSSolver"},{"category":"section","location":"solvers/#Choosing-a-Solver","page":"Solvers","text":"Use case Recommended solver\nDefault / first try LAML\nAutomatic smoothing comparison GCVSolver\nStiff or chaotic systems CollocationLAML\nQuick baseline TwoStageSolver\nIntegration-free derivative matching GradientMatching\nGP-based gradient matching (MAP or MCMC) AdaptiveGradientMatching\nEnsemble gradient matching with uncertainty BNGSolver\nNeural network approximators AdamSolver\nRobust neural fitting MultipleShootingSolver\nWhen gradients fail DerivativeFreeSolver\nUncertainty quantification MCMCSolver or MagiSolver\nExact posterior accounting for ODE-solver error PseudoMarginalSolver\nFast approximate Bayesian VariationalSolver\nIntractable likelihood ABCSolver\nProbabilistic numerics RodeoSolver or DaltonSolver\nNoisy data, integration-free IntegralMatchingSolver\nIdentifiability analysis ProfileLikelihoodSolver\nDerivative-free batch estimation EnsembleKalmanSolver\nGP-weighted gradient matching ODINSolver\nTrajectory-in-RKHS gradient matching RKHSSolver","title":"Choosing a Solver"},{"category":"section","location":"solvers/#Missing-and-Masked-Observations","page":"Solvers","text":"An observation cell is masked — excluded from every objective, every reported loss and every denominator — when either\n\nits value is non-finite (data_values[i, j] = NaN), or\nits weight is zero (data_weights[i, j] = 0.0).\n\nMarking a cell both ways is the recommended convention, and the one every example and test in this package follows:\n\ndata_values[3, 1]  = NaN    # the observation is missing\ndata_weights[3, 1] = 0.0    # ...and carries no weight\n\nA zero weight alone is not sufficient protection against a NaN value: IEEE arithmetic gives 0 * NaN = NaN, so an ungated weighted sum turns the entire objective — and every gradient derived from it — into NaN. That is why the package gates on the cell predicate (positive weight and finite value) rather than on the weight alone.\n\nMasking is useful for ragged panels (states observed on different schedules), dropped or censored samples, and hold-out cells for cross-validation, without having to reshape data_times/data_values.","title":"Missing and Masked Observations"},{"category":"section","location":"solvers/#Which-solvers-support-masking","page":"Solvers","text":"Masked data is supported by all but four solvers: LAML, GCVSolver, CollocationLAML, GradientMatching, AdaptiveGradientMatching, TwoStageSolver, BNGSolver, ODINSolver, RKHSSolver, IntegralMatchingSolver, AdamSolver, MultipleShootingSolver, DerivativeFreeSolver, MCMCSolver, MagiSolver, VariationalSolver, ABCSolver and ProfileLikelihoodSolver.\n\nFour solvers reject masked data with an error:\n\nRodeoSolver\nDaltonSolver\nPseudoMarginalSolver\nEnsembleKalmanSolver\n\nTheir data term lives inside a Kalman-filter or particle recursion that has no per-cell mask. Honouring a mask there means skipping the filter update, not merely the density term, for the masked cells — a structural change to those recursions. Left unguarded they do not merely report NaN: DaltonSolver flattens its objective to a constant, PseudoMarginalSolver rejects every Metropolis–Hastings proposal, and EnsembleKalmanSolver turns every ensemble member NaN on the first iteration — each of which returns the initialization while looking like an ordinary converged fit. Failing loudly is the point.\n\nTo use one of these four with incomplete data, drop the masked rows from data_times/data_values before constructing the PSMProblem.","title":"Which solvers support masking"},{"category":"section","location":"solvers/#Diagnostics","page":"Solvers","text":"residual_diagnostics and appraise exclude masked cells from every statistic. Because a PSMSolution stores data_values but not data_weights, they detect masking from the stored value only — one more reason to mark a masked cell with NaN as well as with a zero weight.","title":"Diagnostics"},{"category":"type","location":"solvers/#PartiallySpecifiedModels.LAML","page":"Solvers","text":"LAML(; maxiters=100, tol=1e-6, verbose=false, initial_lambda=nothing,\n       warmup=3, sigma2_init=nothing)\n\nLaplace Approximate Marginal Likelihood algorithm. Equivalent to REML for Gaussian data. Uses Fellner-Schall + Newton for smoothing parameter estimation.\n\nKeyword arguments\n\nmaxiters::Int=100: maximum IRLS+LAML iterations\ntol::Float64=1e-6: convergence tolerance on penalized objective\nverbose::Bool=false: print iteration diagnostics\ninitial_lambda::Union{Nothing,Float64}=nothing: initial smoothing parameter for all terms.  Default (nothing) uses the data-driven θ = 1/tr(S) per term (values below λ ≈ 4.5e-5 are floored there when LAML starts, to keep the criterion away from its flat tiny-λ region).  For strongly nonlinear problems, a higher value (e.g. 10.0) combined with warmup helps the IRLS converge to a good basin before LAML refinement.\nwarmup::Int=3: number of IRLS iterations to run with fixed smoothing before engaging LAML estimation.  Allows the coefficient estimates to stabilise before the smoothing parameters are adapted.  Increase for strongly nonlinear models (e.g. warmup=10).\nsigma2_init::Union{Nothing,Float64}=nothing: cap on the profiled σ² used in the Fellner-Schall smoothing update during the warmup phase.  When provided, σ² is clamped to min(profiled_σ², sigma2_init), preventing the large residual variance from an early poor fit from driving oversmoothing.  After the warmup phase the cap is progressively relaxed.  Set to a value reflecting your prior belief about observation noise variance (e.g. sigma2_init=25.0 for ±5 measurement error).\n\nConvergence info\n\nsol.convergence is a NamedTuple (V_beta, sigma2, converged, iterations, reason, laml_failures): the posterior covariance and σ̂² used by confidence_band, plus the standard honest-convergence keys (see PSMSolution) and laml_failures::Int, the number of iterations in which the LAML smoothing-parameter update failed and θ was kept.\n\n\n\n\n\n","title":"PartiallySpecifiedModels.LAML"},{"category":"type","location":"solvers/#PartiallySpecifiedModels.GCVSolver","page":"Solvers","text":"GCVSolver\n\nGeneralized Cross-Validation solver for smoothing parameter selection.\n\nUses GCV score minimization (via golden-section search on log(λ)) as an alternative to LAML/REML. Simpler and faster than LAML, but typically produces slightly less smooth estimates.\n\nFields\n\nn_grid: number of grid points for initial λ search (default 50)\nmaxiters: maximum IRLS iterations (default 50)\ntol: convergence tolerance (default 1e-6)\ngamma: GCV inflation factor (default 1.4, >1 guards against under-smoothing; gamma=1.0 reproduces the classical unmodified GCV of Wood (2001) / ddefit)\nverbose: print progress\n\nConvergence info\n\nsol.convergence is a NamedTuple (converged, iterations, reason, gcv) with the standard honest-convergence keys (see PSMSolution) plus gcv::Float64, the last GCV score (NaN when no smooth terms are present).\n\n\n\n\n\n","title":"PartiallySpecifiedModels.GCVSolver"},{"category":"type","location":"solvers/#PartiallySpecifiedModels.CollocationLAML","page":"Solvers","text":"CollocationLAML(; kwargs...)\n\nCollocation-based LAML solver using the generalized profiling / parameter cascading approach of Ramsay et al. (2007).\n\nInstead of integrating the ODE at each step (as LAML does), this solver represents the state trajectories as free parameters and penalizes deviation from the ODE.  A continuation schedule gradually increases the ODE compliance penalty λ_ode, transitioning from a data-fitting problem (flexible states) to a model-constrained problem (states satisfy ODE).\n\nThis approach is much more robust for highly nonlinear or oscillatory models (e.g., Lotka–Volterra) where the standard IRLS linearization fails because the ODE trajectory is extremely sensitive to parameter changes.  See Fasiolo, Pya & Wood (2016), Statistical Science 31(1).\n\nKeyword arguments\n\nmaxiters::Int=50: IRLS iterations per continuation level\ntol::Float64=1e-6: convergence tolerance\nverbose::Bool=false: print diagnostics\nlambda_ode_start::Float64=0.01: initial ODE compliance penalty\nlambda_ode_end::Float64=1e4: final ODE compliance penalty\nn_continuation::Int=8: number of log-spaced continuation levels\nsigma2_init::Union{Nothing,Float64}=nothing: σ² cap for Fellner-Schall\n\nnote: ODE problems only\nDDE problems are REJECTED with an error. The collocation objective calls the dynamics through the 4-argument ODE signature and has no way to supply the delayed history, so a DDE would silently be fitted as though it had no delays. Use LAML for DDEs.\n\nConvergence info\n\nsol.convergence is a NamedTuple (ode_compliance, lambda_ode_final, converged, iterations, reason, iterations_total). converged/reason describe the final continuation level's inner loop, iterations counts its inner iterations, and iterations_total accumulates inner iterations across all continuation levels (see PSMSolution for the key taxonomy).\n\n\n\n\n\n","title":"PartiallySpecifiedModels.CollocationLAML"},{"category":"type","location":"solvers/#PartiallySpecifiedModels.GradientMatching","page":"Solvers","text":"GradientMatching(; maxiters=500, tol=1e-6, verbose=false, sigma2_init=nothing)\n\nTwo-step gradient matching solver inspired by NODEBNGM (Bonnaffé et al. 2023):\n\nSmooth observed data with cubic splines to obtain ŷ(t) and dŷ/dt\nFit unknown function parameters by matching ODE derivatives: minimize ||dŷ/dt - f(ŷ, p, t)||² + penalty\n\nAvoids ODE integration entirely, making it far more robust for neural network approximators where the IRLS linearization of the ODE trajectory is poor.\n\nUses Gauss-Newton for penalized approximators (B-spline, GP) and Adam optimizer for unpenalized approximators (neural networks).\n\nRequires that all state variables are observed (no latent states).\n\nArguments\n\nmaxiters::Int=500: maximum iterations (Adam needs more than GN)\ntol::Float64=1e-6: convergence tolerance\nverbose::Bool=false: print iteration details\nsigma2_init::Union{Nothing,Float64}=nothing: σ² cap for Fellner-Schall\n\nnote: σ² for the Fellner-Schall update\nThe residual variance driving the smoothing-parameter update is σ̂² = RSS / #{residuals with nonzero weight}, not RSS / #residuals. Unobserved-state rows are zero-weighted; counting them biased σ̂² low and over-smoothed the unknown functions.\n\n\n\n\n\n","title":"PartiallySpecifiedModels.GradientMatching"},{"category":"type","location":"solvers/#PartiallySpecifiedModels.TwoStageSolver","page":"Solvers","text":"TwoStageSolver\n\nTwo-stage smooth-then-differentiate solver (simplest baseline).\n\nStage 1: Smooth each observed state independently via spline + GCV/penalty. Stage 2: Numerically differentiate smoothed curves, then match ODE RHS           to derivatives via least squares to infer unknown function params.\n\nThis is the original approach from Wood (2001) / deGradInfer (Macdonald & Husmeier 2015).\n\nFields\n\nn_basis_smooth: upper limit on the number of B-spline coefficients used by the stage-1 smoother; the actual basis size is clamp(n − 2, 4, n_basis_smooth) for n data points (default 15).\nnote: Changed\nThis field was previously dead: it was documented (default 20) but never read — the smoother always capped the basis at 15. It is now wired through; the default was set to 15 so default fits are byte-identical to before.\nlambda_smooth: smoothing penalty for initial data fit (default 1.0)\nmaxiters: max iterations for parameter matching (default 1000)\nlr: learning rate for Adam optimization in matching step (default 0.01)\nverbose: print progress\n\nConvergence info\n\nsol.convergence is a NamedTuple (converged, iterations, reason, method) with the standard honest-convergence keys (see PSMSolution); converged=true with reason=:plateau when the matching loss stagnated over a 30-iteration window, reason=:maxiters otherwise.\n\n\n\n\n\n","title":"PartiallySpecifiedModels.TwoStageSolver"},{"category":"type","location":"solvers/#PartiallySpecifiedModels.AdaptiveGradientMatching","page":"Solvers","text":"AdaptiveGradientMatching(; maxiters=200, verbose=false, gamma_init=1.0,\n                           fit_gamma=true, kernel=:rbf, n_samples=0,\n                           n_chains=10, rng_seed=nothing)\n\nAdaptive Gradient Matching solver using the product-of-experts objective of Dondelinger et al. (2013). Gaussian processes smooth the data and supply gradient estimates with uncertainty; the ODE-predicted gradients are matched under the product-of-experts likelihood.\n\nTwo modes:\n\nMAP (default, n_samples=0): GP hyperparameters from a marginal-likelihood grid search, latent states fixed at the GP posterior mean, and (β, log γ) optimized once by L-BFGS. Fast; no posterior samples.\nPopulation MCMC (n_samples > 0): the tempered population-MCMC sampler of Dondelinger et al./deGradInfer. n_chains chains at temperatures t_c = ((c−1)/(C−1))⁵ jointly sample the latent states X, the parameters β, and the mismatch variances γ by adaptive blockwise random-walk Metropolis with exchange moves between adjacent chains; the ODE-mismatch likelihood is raised to t_c, interpolating from pure GP regression (t=0) to the fully coupled model (t=1). The cold chain (t=1) provides the posterior: reported unknown functions use the posterior-mean β, and sol.convergence.beta_samples / gamma_samples hold the draws. GP kernel hyperparameters stay fixed at their grid-search values (a documented simplification — the paper also samples them). Continuous-time problems only.\n\nThe (log-)likelihood term for each state k is:     Lk = -0.5 (fk - mk)ᵀ (Ak + γk I)⁻¹ (fk - mk) - 0.5 log|Ak + γ_k I|\n\nwhere:\n\nf_k = ODE-predicted gradients for state k\nmk = GP gradient mean ('K K⁻¹ xk, conditioned on the states)\nA_k = GP gradient covariance = K** - K(K + σ²I)⁻¹Kᵀ\nγ_k = mismatch parameter controlling ODE-GP coupling\n\nArguments\n\nmaxiters::Int=200: maximum L-BFGS iterations (MAP mode); MCMC sweeps are 2 n_samples (half burn-in) in sampling mode\nverbose::Bool=false: print iteration details\ngamma_init::Float64=1.0: initial mismatch parameter (per state)\nfit_gamma::Bool=true: optimize γ or keep fixed (MAP mode)\nkernel::Symbol=:rbf: GP kernel (:rbf, :matern32)\nn_samples::Int=0: cold-chain posterior draws; 0 = MAP mode\nn_chains::Int=10: number of tempered chains (sampling mode; ≥ 2)\nrng_seed: seed for the sampler (default nothing = non-reproducible)\n\nnote: GP centering\nEach state's GP prior is centered: observed states on their data mean, unobserved states on their initial condition, and every x entering the GP quadratic forms and the gradient mean is the CENTERED state x − m_k. A zero-mean GP prior on uncentered data pulls the trajectory toward zero, which biased the fitted unknown functions on data far from the origin.\n\n\n\n\n\n","title":"PartiallySpecifiedModels.AdaptiveGradientMatching"},{"category":"type","location":"solvers/#PartiallySpecifiedModels.BNGSolver","page":"Solvers","text":"BNGSolver\n\nEnsemble Bayesian gradient matching (Bonnaffé & Coulson 2023). Avoids ODE integration entirely: observed series are smoothed with GCV splines, and unknown-function parameters are fit by matching the smoothed derivatives under a variance-marginalized log-posterior (n/2)log(1 + SSR/2) + (|β|/2)log(1 + Σ(β/prior_sd)²/2) (the paper's \"Bayesian regularisation\": observation and prior variances are marginalized, not supplied).\n\nUncertainty comes from a k_obs × k_proc fit ensemble: k_obs observation resamples (residual bootstrap of the smoother; the first is the original data) times k_proc restarts from perturbed initialisations. Reported unknown functions are ensemble means; sol.convergence.ensemble_std[name] gives the pointwise ensemble standard deviation.\n\nFields\n\nk_obs: observation-ensemble size (default 10)\nk_proc: process fits per observation ensemble (default 3)\nprior_sd: prior scale of the marginalized Gaussian parameter prior (default 10.0)\nmaxiters: Adam iterations per ensemble member (default 2000)\nlr: Adam learning rate (default 0.01)\nlambda_smooth: extra smoothing penalty on approximator coefficients (default 1.0)\nrng_seed: seed for the bootstrap and restarts (default nothing = non-reproducible)\nverbose: print progress\n\nConvergence info\n\nsol.convergence carries the standard honest-convergence keys (see PSMSolution): converged/reason report the BEST ensemble member's outcome (:plateau when its loss stagnated, :maxiters otherwise), iterations is the total Adam iterations summed over all ensemble members, and member_converged::Vector{Bool} records each member's plateau flag (alongside the existing n_ensemble, member_losses, member_weights, ensemble_std keys).\n\nReferences\n\nBonnaffé & Coulson (2023), \"Fast fitting of neural ordinary differential equations by Bayesian neural gradient matching to infer ecological interactions from time-series data\", Methods Ecol Evol 14\n\n\n\n\n\n","title":"PartiallySpecifiedModels.BNGSolver"},{"category":"type","location":"solvers/#PartiallySpecifiedModels.AdamSolver","page":"Solvers","text":"AdamSolver(; maxiters=300, lr=0.01, verbose=false, loss=:auto,\n             penalty_weight=0.0, autodiff=true)\n\nAdam optimizer that trains unknown function parameters through ODE integration.\n\nFor neural networks: uses a ForwardDiff-compatible MLP evaluator (bypassing Lux) so that exact gradients can be computed through the ODE solve. This matches the approach used in Universal Differential Equations (UDEs).\n\nFor B-splines and GPs: also supported, uses the standard evaluators.\n\nThe loss function computes solve(ODEProblem(...), solver) at each step and compares with data. ForwardDiff computes exact gradients through the ODE solve.\n\nUnlike LAML/GCVSolver, this solver performs no automatic smoothing-parameter selection: with the default penalty_weight = 0 the fit is unpenalized (spline coefficients are free). Set penalty_weight > 0 to add the fixed quadratic roughness penalty penalty_weight · Σₖ βₖ' Sₖ βₖ to the loss.\n\nArguments\n\nmaxiters::Int=300: maximum Adam iterations\nlr::Float64=0.01: learning rate (with cosine annealing)\nverbose::Bool=false: print iteration details\nloss::Symbol=:auto: loss function; :auto selects :mse for Gaussian and :poisson for Poisson likelihoods (other families are not supported by this solver — use LAML). Explicit :mse/:poisson override with a warning on mismatch.\npenalty_weight::Float64=0.0: fixed weight of the quadratic smoothing penalty added to the loss (0 disables)\nautodiff::Bool=true: use ForwardDiff (true) or finite differences (false)\n\nConvergence info\n\nsol.convergence is a NamedTuple (optimizer, method, converged, iterations, reason, final_grad_norm) with the standard honest-convergence keys (see PSMSolution) plus final_grad_norm::Float64, the Euclidean norm of the last computed gradient. reason == :plateau is only reported while the cosine-annealed learning rate is still above 5% of the base lr — a plateau that appears merely because the schedule has driven the step size to zero is reported as :maxiters, not convergence.\n\n\n\n\n\n","title":"PartiallySpecifiedModels.AdamSolver"},{"category":"type","location":"solvers/#PartiallySpecifiedModels.MultipleShootingSolver","page":"Solvers","text":"MultipleShootingSolver(; n_intervals=10, maxiters_inner=100, maxiters_outer=20,\n                         rho_init=10.0, rho_max=1e6, loss=:auto,\n                         penalty_weight=0.0, verbose=false)\n\nMultiple shooting solver for training neural differential equations, following Turan & Jäschke (2021). Partitions the time span into intervals with shooting variables at boundaries. Uses an augmented Lagrangian (L-BFGS inner subproblems) to enforce continuity.\n\nAdvantages over single shooting (AdamSolver):\n\nBetter initial fits: shooting variables initialized from data\nAvoids \"flattened trajectory\" failure mode for oscillatory systems\nShorter integration intervals improve gradient quality\n\nArguments\n\nn_intervals::Int=10: number of shooting intervals\nmaxiters_inner::Int=100: L-BFGS iterations per augmented Lagrangian step\nmaxiters_outer::Int=20: augmented Lagrangian outer iterations\nrho_init::Float64=10.0: initial penalty parameter for shooting constraints\nrho_max::Float64=1e6: maximum penalty parameter\nloss::Symbol=:auto: data-fit loss. :auto follows prob.likelihood (Gaussian → :mse weighted SSE, Poisson → :poisson weighted negative log-likelihood kernel; other families error). Explicit :mse/:poisson are honored with a warning on mismatch, matching AdamSolver.\npenalty_weight::Float64=0.0: fixed quadratic smoothing penalty penalty_weight · Σₖ βₖ'Sₖβₖ added to the training objective (0 = no penalty; there is no smoothing-parameter selection here)\nverbose::Bool=false: print iteration details\n\nConvergence info\n\nsol.convergence is a NamedTuple (optimizer, method, n_intervals, converged, iterations, reason, max_gap, rho_final) with the standard honest-convergence keys (see PSMSolution; iterations counts outer augmented-Lagrangian iterations) plus max_gap::Float64, the final maximum shooting-gap magnitude, and rho_final::Float64, the final penalty parameter.\n\n\n\n\n\n","title":"PartiallySpecifiedModels.MultipleShootingSolver"},{"category":"type","location":"solvers/#PartiallySpecifiedModels.DerivativeFreeSolver","page":"Solvers","text":"DerivativeFreeSolver\n\nDerivative-free optimization solver using NelderMead or particle swarm.\n\nThe objective includes the quadratic roughness penalty 0.5 · penalty_weight · Σₖ βₖ'Sₖβₖ (note the ½ factor; default penalty_weight=1.0; set penalty_weight=0 for an unpenalized fit — there is no smoothing-parameter selection here).\n\nUseful as a robust fallback when gradient-based methods fail (non-smooth objectives, stiff dynamics, poor conditioning). Uses simulation-based loss without requiring autodiff through ODE solves.\n\nFields\n\nmethod: optimization method — :nelder_mead or :particle_swarm (default :nelder_mead)\nmaxiters: maximum function evaluations (default 10000)\nn_particles: particle count for swarm methods (default 20)\nloss: loss type (default :auto). :auto follows prob.likelihood — Gaussian → :mse (weighted SSE), any other family → :likelihood (the family's negative log-likelihood). Explicit :mse or :likelihood is honored regardless of the likelihood family.\npenalty_weight: weight of the roughness penalty above (default 1.0)\nverbose: print progress\n\n\n\n\n\n","title":"PartiallySpecifiedModels.DerivativeFreeSolver"},{"category":"type","location":"solvers/#PartiallySpecifiedModels.RodeoSolver","page":"Solvers","text":"RodeoSolver\n\nProbabilistic ODE solver (rodeo) for parameter inference.\n\nUses Kalman filtering with an integrated Brownian motion prior to approximate the ODE solution and compute an approximate marginal likelihood.\n\nFields\n\nn_steps: number of solver discretization steps (default: 200)\nn_deriv: number of derivatives in IBM prior (default: 3)\nsigma: IBM scale parameters (one per state variable, or nothing for auto)\nobs_var: observation noise variance (or nothing for auto)\nmethod: likelihood approximation (:basic or :fenrir)\ninterrogate: interrogation method (:kramer or :schober)\nmaxiters: max L-BFGS iterations (default: 200)\nverbose: print progress (default: false)\n\n\n\n\n\n","title":"PartiallySpecifiedModels.RodeoSolver"},{"category":"type","location":"solvers/#PartiallySpecifiedModels.DaltonSolver","page":"Solvers","text":"DaltonSolver\n\nData-Adaptive Likelihood with Transformed Observations (DALTON) solver.\n\nExtends the probabilistic ODE approach (RODEO) with a data-adaptive marginal likelihood: p(Y|Z) = p(Y,Z)/p(Z), computed via two Kalman filter passes — one joint (ODE + observations) and one marginal (ODE only).\n\nFields\n\nn_steps: number of discretization steps (default 200)\nn_deriv: IBM prior derivative order (default 3)\nsigma: IBM scale parameters (nothing = auto-estimate)\nobs_var: observation noise variance. nothing (the default) auto-estimates it from the data at solve time as 1% of the mean per-column data variance (the same estimator RodeoSolver and PseudoMarginalSolver use). A fixed numeric value is honored exactly; note a fixed value must be chosen relative to the data scale — a value appropriate for data of order 1 badly misfits data of order 1000.\ninterrogate: interrogation method :kramer or :schober (default :kramer)\nmaxiters: optimization iterations (default 200)\nverbose: print progress\n\n\n\n\n\n","title":"PartiallySpecifiedModels.DaltonSolver"},{"category":"type","location":"solvers/#PartiallySpecifiedModels.MCMCSolver","page":"Solvers","text":"MCMCSolver(; n_samples=1000, n_warmup=500, n_chains=1, target_accept=0.8,\n             prior_scale=1.0, obs_sigma=nothing, sample_smoothing=false, verbose=false)\n\nFull Bayesian inference via Hamiltonian Monte Carlo (NUTS). Uses LogDensityProblems.jl + AdvancedHMC.jl.\n\nArguments\n\nn_samples: number of posterior samples per chain (after warmup)\nn_warmup: number of warmup/adaptation steps\nn_chains: number of independent chains\ntarget_accept: target acceptance rate for NUTS adaptation (0.6–0.95)\nprior_scale: scale for Gaussian prior on parameters (larger = weaker prior). When penalty matrices exist (B-spline, GP), uses the penalty; otherwise N(0, prior_scale²).\nobs_sigma: observation noise std dev (Gaussian likelihoods only; errors otherwise). If nothing, sampled as a parameter for Gaussian data; non- Gaussian families sample no σ (their dispersion is fixed in the family object). The data term follows prob.likelihood in all cases.\nsample_smoothing: if true, jointly sample log(λ) for each smooth term with a weakly informative N(log(λ_init), 2²) hyperprior. This gives wider, more honest credible intervals for the unknown functions. Default: false.\nverbose: print progress\n\n\n\n\n\n","title":"PartiallySpecifiedModels.MCMCSolver"},{"category":"type","location":"solvers/#PartiallySpecifiedModels.MagiSolver","page":"Solvers","text":"MagiSolver(; n_samples=1000, n_warmup=500, n_gridpoints=200,\n             sigma=nothing, obs_var=nothing, target_accept=0.8,\n             prior_scale=1.0, preoptimize=true, verbose=false)\n\nManifold-constrained Gaussian process inference (MAGI) for ODE systems. Gaussian likelihoods only (the manifold-constrained posterior assumes Gaussian observation noise); non-Gaussian prob.likelihood errors.\n\nMAGI places a Matérn-3/2 Gaussian-process prior on each state (the MAGI paper uses a generalized Matérn kernel with ν ≈ 2.01; with ν = 3/2 the implied derivative process is rougher — mean-square continuous but not mean-square differentiable) and constrains the GP-implied derivative to the ODE vector field through the conditional derivative covariance K* = ''K − 'K C⁻¹ ('K)ᵀ. The state values on the discretization grid are sampled jointly with the unknown-function parameters θ via NUTS/HMC (the manifold-constrained posterior).\n\nKey advantage: Handles partially observed systems naturally (unobserved state components are inferred through the ODE constraint).\n\nReturns an MCMCChains.Chains object with posterior samples.\n\nFields\n\nn_samples: number of posterior samples after warmup\nn_warmup: warmup/adaptation iterations\nn_gridpoints: number of discretization grid points for the GP/manifold constraint\nsigma: per-state observation noise standard deviations (one entry per state component). When supplied, these fixed SDs are used in the data term instead of auto-estimation; mutually exclusive with obs_var.\nobs_var: observation noise variance shared across components; nothing (default) estimates it from the data via a local-linear residual estimator (a fixed scale-blind default distorted the posterior whenever the data's units differed from O(0.1))\ntarget_accept: NUTS target acceptance rate\nprior_scale: scale for Gaussian prior on parameters\npreoptimize: run a short MAP pre-optimization to initialize the sampler (default true)\nverbose: print progress\n\nnote: Changed\nThe former n_deriv field was removed: it was never read anywhere (the Matérn-3/2 GP prior has no derivative-order setting), so keeping it only suggested a control that did not exist.\n\nReferences\n\nYang, Wong & Kou (2021) PNAS 118(15): \"Inference of dynamic systems from noisy and sparse data via manifold-constrained Gaussian processes\"\n\n\n\n\n\n","title":"PartiallySpecifiedModels.MagiSolver"},{"category":"type","location":"solvers/#PartiallySpecifiedModels.PseudoMarginalSolver","page":"Solvers","text":"PseudoMarginalSolver\n\nPseudo-marginal MCMC using a probabilistic ODE solver for likelihood estimation.\n\nUses RODEO/fenrir as an inner solver to compute an unbiased estimate of the marginal likelihood p(Y|θ), then samples from the posterior p(θ|Y) via adaptive random-walk Metropolis (the proposal scale is tuned toward target_accept during warmup).\n\nFields\n\nn_samples: number of posterior samples (default 1000)\nn_warmup: warmup/adaptation samples (default 500)\nn_steps: discretization steps for inner probabilistic solver (default 200)\nn_deriv: IBM prior derivative order (default 3)\nsigma: IBM scale parameters (nothing = auto-estimate)\nobs_var: observation noise variance (default nothing = estimate it from the data alongside the other nuisance quantities)\ntarget_accept: target acceptance rate for the adaptive random-walk Metropolis proposal-scale adaptation (default 0.8)\nprior_scale: prior VARIANCE on parameters (default 1.0). Used directly as the variance of the Gaussian smoothing prior exp(−½ βₖ'Sₖβₖ / prior_scale) and of the weak ridge exp(−½ ‖β‖² / (100·prior_scale)) — it is not squared.\ninner_method: likelihood estimator — :ffbs (default; unbiased FFBS Monte-Carlo average, giving genuine pseudo-marginal MCMC), :fenrir (deterministic Fenrir evidence; the chain is then plain adaptive RWM on an approximate likelihood), or :dalton (deterministic DALTON data-adaptive likelihood, likewise plain RWM)\nverbose: print progress\n\n\n\n\n\n","title":"PartiallySpecifiedModels.PseudoMarginalSolver"},{"category":"type","location":"solvers/#PartiallySpecifiedModels.VariationalSolver","page":"Solvers","text":"VariationalSolver\n\nVariational inference solver using mean-field Gaussian approximation.\n\nApproximates the posterior p(θ|Y) with a factored Gaussian q(θ) = ∏ N(μᵢ, σᵢ²) by maximizing the evidence lower bound (ELBO). Much faster than MCMC while providing uncertainty estimates.\n\nFields\n\nmaxiters: max ELBO optimization iterations (default 2000)\nlr: learning rate for Adam on ELBO (default 0.01)\nn_elbo_samples: Monte Carlo samples for ELBO gradient (default 10)\nprior_scale: prior VARIANCE on parameters (default 1.0). Used directly as the variance of the Gaussian smoothing prior — the prior precision is Λ = ridge·I + Σₖ Sₖ / prior_scale — it is not squared.\nobs_noise_var: Gaussian observation-noise variance (default nothing = estimate from the data). Gaussian likelihoods only; errors otherwise. Non-Gaussian families use their own pointwise log-likelihood in the ELBO.\nverbose: print progress\n\nnote: Reported EDF\nsol.edf is the Laplace effective degrees of freedom at the variational mean, tr((H + Λ)⁻¹H) with the Gauss–Newton information H = JᵀWJ/σ²_obs — a real measure of model complexity that responds to the smoothing level. It is NaN (honestly missing) when the model cannot be simulated at the variational mean or the linear system is singular; it is never a fabricated constant.\n\n\n\n\n\n","title":"PartiallySpecifiedModels.VariationalSolver"},{"category":"type","location":"solvers/#PartiallySpecifiedModels.ABCSolver","page":"Solvers","text":"ABCSolver\n\nApproximate Bayesian Computation with Sequential Monte Carlo (ABC-SMC).\n\nLikelihood-free inference using simulation-based rejection sampling with adaptive tolerance scheduling. Works for any simulator, including those where the likelihood is intractable.\n\nPrior (prior keyword): :smoothness (default) is a Gaussian GMRF built from the approximators' roughness penalties, centered on their initial values with overall spread prior_scale — matching the prior structure of MCMCSolver/VariationalSolver, so wiggly coefficient vectors are a-priori discouraged. :box restores the legacy uniform box of half-width prior_scale (posterior then depends strongly on the initialization; treat as approximate).\n\nFields\n\nn_particles: number of ABC particles (default 500)\nn_generations: number of SMC generations (default 10)\nsummary_fn: summary statistic function, or :auto for MSE-based (default :auto)\nprior: :smoothness (default, GMRF) or :box (legacy uniform)\nprior_scale: prior spread — GMRF scale, or box half-width (default 2.0)\nquantile_eps: quantile for tolerance schedule (default 0.5)\nverbose: print progress\n\n\n\n\n\n","title":"PartiallySpecifiedModels.ABCSolver"},{"category":"type","location":"solvers/#PartiallySpecifiedModels.IntegralMatchingSolver","page":"Solvers","text":"IntegralMatchingSolver\n\nIntegral matching estimator: integrates both sides of the ODE so that parameter estimation requires neither derivative estimation nor repeated ODE integration.  Compares smoothed trajectory increments ŷ(tᵢ)−ŷ(t₁) against the cumulative trapezoidal integral of f(ŷ(s),p,s).\n\nFields\n\nlambda_smooth: smoothing penalty weight (default 1.0)\nmaxiters: Adam iterations (default 1000)\nlr: learning rate (default 0.01)\nverbose: print progress\n\nConvergence info\n\nsol.convergence is a NamedTuple (converged, iterations, reason, method) with the standard honest-convergence keys (see PSMSolution); converged=true with reason=:plateau when the matching loss stagnated over a 30-iteration window, reason=:maxiters otherwise.\n\nReferences\n\nDattner & Klaassen (2015), EJS 9(2), 1939–1973\nR package simode (Yaari & Dattner)\n\n\n\n\n\n","title":"PartiallySpecifiedModels.IntegralMatchingSolver"},{"category":"type","location":"solvers/#PartiallySpecifiedModels.ProfileLikelihoodSolver","page":"Solvers","text":"ProfileLikelihoodSolver\n\nProfile likelihood for identifiability analysis and confidence intervals.\n\nFor each unknown-function parameter βⱼ, sweeps βⱼ over a grid while optimising all other parameters (β₋ⱼ) at each grid point.  Returns the profile likelihood curve Lₚ(βⱼ) and a likelihood-ratio CI.\n\nThe profile is taken through the penalized objective at the fitted smoothing parameters λ̂ (a penalized spline is not identified through its raw RSS); the statistic ΔPenSS/σ̂² is referenced against χ²₁, and CI endpoints are interpolated between grid points. Nuisance coefficients are re-optimised at each grid point by a long Nelder–Mead run, warm-started from the previous grid point. Gaussian likelihoods only.\n\nFields\n\nn_profile_points: grid points per parameter (default 20); the exact MLE value is inserted, so the evaluated grid may have one extra point\nci_level: confidence level for LR-based CI (default 0.95)\nparam_indices: which parameter indices to profile (default nothing = all)\nverbose: print progress\nbase_alg: the LAML fit the profile is taken around (default LAML(verbose=false)). The returned solution reports THIS fit's convergence keys, so a deliberately truncated base — e.g. LAML(maxiters=1) — surfaces as converged = false rather than being laundered into a converged profile-likelihood result.\n\nReferences\n\nSimpson & Maclaren (2023), PLOS Comp Biol\n\n\n\n\n\n","title":"PartiallySpecifiedModels.ProfileLikelihoodSolver"},{"category":"type","location":"solvers/#PartiallySpecifiedModels.EnsembleKalmanSolver","page":"Solvers","text":"EnsembleKalmanSolver\n\nEnsemble Kalman Inversion (EKI) for batch parameter estimation.\n\nUses an ensemble of parameter particles, propagates each through the forward model (ODE simulation), and updates via the Kalman gain K = Cov(θ, G(θ)) [Cov(G(θ), G(θ)) + Γ]⁻¹.  Iterates until the ensemble collapses around the MAP estimate — mean particle std below 1e-3 of its initial value, reported as converged = true, reason = :ensemble_collapse — or until n_iterations is exhausted, reported honestly as converged = false, reason = :maxiters.\n\nDiscrete problems are propagated with simulate_discrete (unit steps over tspan, data times snapped to the nearest integer step), so EKI sees the same trajectory as every other solver.\n\nFields\n\nn_ensemble: ensemble size (default 50)\nn_iterations: EKI iterations (default 30)\nnoise_scale: observation noise std for regularisation (default 0.1). This is a FIXED scale, not auto-estimated from the data: set it relative to the magnitude of your observations (e.g. roughly the expected noise std). The default suits data of order 1; for data of order 1000 use a correspondingly larger value.\nverbose: print progress\n\nReferences\n\nIglesias, Law & Stuart (2013), Inverse Problems\nSchillings & Stuart (2017), SIAM J Numer Anal\n\n\n\n\n\n","title":"PartiallySpecifiedModels.EnsembleKalmanSolver"},{"category":"type","location":"solvers/#PartiallySpecifiedModels.ODINSolver","page":"Solvers","text":"ODINSolver\n\nODE-informed regression (Wenk, Abbati et al. 2020). GP hyperparameters are first estimated per observed state by marginal likelihood, then the states X and the unknown-function parameters θ are optimised jointly against the ODIN risk\n\nR(X, θ) = Σ_k [ ‖y_k − x_k‖²/σ_{n,k}² + x̃_kᵀ K_k⁻¹ x̃_k\n                + (f_k(X,θ) − D_k x̃_k)ᵀ A_k⁻¹ (f_k(X,θ) − D_k x̃_k) ],\n\nwhere x̃_k is the centered state, D_k = 'K K⁻¹ maps states to their GP conditional-mean derivative, and A_k = ''K − 'K K⁻¹ 'Kᵀ + γI is the GP conditional derivative covariance — so the ODE mismatch is trusted only in directions the GP actually determines. Unobserved states are included as free variables (GP prior + ODE terms, no data term), so partially observed systems are supported.\n\nFields\n\nmaxiters: outer iterations; 20 Adam steps each (default 50)\ngp_lengthscale, gp_variance: nothing (default) estimates the RBF hyperparameters per state by GP marginal likelihood. Supplying BOTH fixes them for all states (with noise assumed at 0.01 * gp_variance); supplying only one is an error\node_weight: extra multiplier on the ODE-mismatch term (default 1.0 = the natural Mahalanobis weighting of the paper)\nlr: Adam learning rate (default 0.01)\nverbose: print progress\n\nConvergence info\n\nsol.convergence is a NamedTuple (converged, iterations, reason, method, gp_hyperparams) with the standard honest-convergence keys (see PSMSolution). iterations counts the joint Adam steps actually performed (the budget is 20 * maxiters steps); converged=true with reason=:plateau when the risk stagnated over a 30-step window while the cosine-annealed learning rate was still above 5% of the base lr, reason=:maxiters otherwise.\n\nsol.data_loss is the WEIGHTED residual sum of squares over usable cells (weighted_data_loss), the same convention as every other solver — so masked (data_weights == 0) and down-weighted observations contribute to the reported misfit exactly in proportion to their weight.\n\nReferences\n\nWenk, Abbati et al. (2020), AAAI — ODIN\nWenk et al. (2019), AISTATS — FGPGM\n\n\n\n\n\n","title":"PartiallySpecifiedModels.ODINSolver"},{"category":"type","location":"solvers/#PartiallySpecifiedModels.RKHSSolver","page":"Solvers","text":"RKHSSolver\n\nTrajectory-RKHS estimator (González et al. 2014): the state trajectory is placed in a time-kernel RKHS, x_k(t) = m_k + Σᵢ b_{k,i} k(t, tᵢ) with centers at the data times, so ẋ_k(t) is available analytically. The objective — data fit + RKHS norm λ bᵀKb + ODE-gradient match ρ Σ(ẋ − f(x,θ))² on a collocation grid — is minimized by alternating a linear solve for the trajectory coefficients (with f frozen at the previous iterate, Picard linearization) and Adam steps on the unknown-function parameters θ. Continuous-time problems only.\n\nFields\n\nkernel: time-kernel type :rbf, :matern32, :matern52 (default :rbf)\nlengthscale: time-kernel lengthscale; ≤ 0 (default) auto-scales to time_span / 8\nlambda_rkhs: RKHS-norm penalty λ on the trajectory (default 0.01)\nlambda_ode: weight ρ of the ODE-gradient term (default 1.0)\nn_repr_points: size of the ODE collocation grid (default 30)\nmaxiters: outer alternations; 10 θ Adam steps each (default 200)\nlr: θ learning rate (default 0.01)\nverbose: print progress\n\nConvergence info\n\nsol.convergence is a NamedTuple (converged, iterations, reason, method, kernel, lengthscale) with the standard honest-convergence keys (see PSMSolution); reason is :converged_tol when the relative objective change fell below tolerance, :maxiters otherwise.\n\nReferences\n\nGonzález et al. (2014), Pattern Recognition Letters\n\n\n\n\n\n","title":"PartiallySpecifiedModels.RKHSSolver"},{"category":"section","location":"vignettes/#Vignettes","page":"Vignettes","text":"The vignettes/ directory contains 35 worked examples covering every solver, approximator, and likelihood type. Each vignette is a self-contained Quarto document with rendered markdown available on GitHub.","title":"Vignettes"},{"category":"section","location":"vignettes/#Getting-Started","page":"Vignettes","text":"# Vignette Description\n01 Getting Started Basic PSM workflow with logistic growth\n02 Likelihoods Gaussian, Poisson, Negative Binomial, Truncated Normal, and custom likelihoods","title":"Getting Started"},{"category":"section","location":"vignettes/#Ecological-Models","page":"Vignettes","text":"# Vignette Description\n03 Lotka–Volterra Hare–lynx predator-prey with LAML and collocation\n04 Copepod 11-stage structured population model with multiple unknown functions\n08 Rosenzweig–MacArthur Recovering functional responses in consumer-resource dynamics\n10 Chemostat Microbial dynamics recovering unknown Monod growth kinetics\n11 Count Data SIR SIR model with Poisson and Negative Binomial likelihoods\n12 Discrete Time Ricker, Beverton–Holt, and discrete competition models\n20 DDE Delay differential equations with unknown functions\n27 Predator-Prey Functional Response Holling Type II with shape constraints, Bayesian and bootstrap CIs\n28 Fisheries Discrete stock-recruitment with Poisson counts and shape constraints","title":"Ecological Models"},{"category":"section","location":"vignettes/#Approximators","page":"Vignettes","text":"# Vignette Description\n05 Neural Networks Comparing B-spline, GP, and neural network approximators\n13 Shape Constraints Monotonicity, convexity, and zero-at-endpoint constraints\n16 COMONet Shape-constrained neural network approximators\n26 SPDE Matérn SPDE approximator with shape constraints and profile range optimization","title":"Approximators"},{"category":"section","location":"vignettes/#Solvers","page":"Vignettes","text":"# Vignette Description\n06 Solver Comparison Side-by-side comparison of eleven solvers\n07 Probabilistic Fitting Probabilistic ODE fitting with uncertainty quantification\n09 Gradient Matching Integration-free inference with adaptive gradient matching\n14 MCMC Full Bayesian inference with HMC/NUTS posterior sampling\n15 MAGI Manifold-constrained Gaussian process inference\n17 BNG Bayesian neural gradient matching\n18 Dalton Data-adaptive Kalman likelihood fitting\n19 Pseudo-Marginal Probabilistic ODE + Bayesian MCMC\n21 GCV Generalized Cross-Validation vs LAML smoothing\n22 Two-Stage Redirect — see Vignette 09\n23 Derivative-Free Nelder-Mead and Particle Swarm optimization\n24 Variational Fast approximate Bayesian inference via variational methods\n25 ABC Likelihood-free inference with ABC-SMC\n31 Integral Matching Noise-robust integration-free estimation\n33 Ensemble Kalman Derivative-free estimation via ensemble Kalman inversion\n34 ODIN ODE-informed Gaussian process regression\n35 RKHS Reproducing kernel Hilbert space estimation","title":"Solvers"},{"category":"section","location":"vignettes/#Uncertainty-Quantification","page":"Vignettes","text":"# Vignette Description\n29 Bootstrap Parametric and nonparametric bootstrap CIs (Gaussian and Poisson)\n30 Model Selection LAML marginal likelihood for knot, approximator, and structural selection\n32 Profile Likelihood Identifiability analysis and likelihood-ratio CIs","title":"Uncertainty Quantification"},{"category":"section","location":"vignettes/#Running-Vignettes-Locally","page":"Vignettes","text":"Each vignette is a Quarto .qmd file that can be rendered locally:\n\ncd vignettes/01_getting_started\nquarto render 01_getting_started.qmd --to html\n\nRequires Quarto and a Julia installation with the package dependencies.","title":"Running Vignettes Locally"},{"category":"section","location":"#PartiallySpecifiedModels.jl","page":"Home","text":"A Julia package for fitting partially specified dynamical models with nonparametric functional responses.","title":"PartiallySpecifiedModels.jl"},{"category":"section","location":"#Overview","page":"Home","text":"Partially specified models (PSMs) are dynamical systems (ODEs, DDEs, or discrete-time maps) in which one or more functional responses are left unspecified and estimated directly from data. Instead of assuming a fixed parametric form for processes like density dependence or predation rates, PSMs replace these unknown functions with flexible nonparametric approximators — penalized B-splines, Gaussian processes, or neural networks — and fit them jointly with the model dynamics.\n\nThis approach is particularly valuable in ecology, where the form of key biological processes (e.g., Holling-type functional responses, density-dependent growth, transmission rates) is often uncertain or debated. PSMs allow researchers to let the data inform the shape of these relationships, combining mechanistic understanding of system structure with statistical flexibility for unknown components.\n\nPartiallySpecifiedModels.jl provides a unified interface for specifying and fitting PSMs using two complementary approximation strategies:\n\nBasis function approximators (B-splines, shape-constrained splines, Gaussian processes): fewer parameters, automatic smoothing via LAML/GCV, interpretable, and easy to constrain (monotonicity, convexity, positivity).\nNeural network approximators (Lux.jl networks, COMONet): more flexible for high-dimensional or complex functional forms, compatible with gradient-based UDE-style training.\n\nThe package supports 22 fitting algorithms, 7 approximator types, 5 likelihood families, and 14 shape constraint types.","title":"Overview"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(url=\"https://github.com/ecorecipes/PartiallySpecifiedModels.jl\")\n\nRequires Julia ≥ 1.10.","title":"Installation"},{"category":"section","location":"#Quick-Start","page":"Home","text":"A simple example: recover the unknown per-capita growth rate r(N) from logistic growth data.\n\nusing PartiallySpecifiedModels\nusing PartiallySpecifiedModels: solve\nusing OrdinaryDiffEq\nusing Random\nRandom.seed!(42)\n\n# Generate synthetic logistic growth data\nr0, K, N0 = 0.5, 10.0, 0.5\ntspan = (0.0, 15.0)\ndata_times = collect(0.0:0.5:15.0)\n\nfunction logistic!(du, u, p, t)\n    du[1] = r0 * (1 - u[1] / K) * u[1]\nend\ntrue_prob = ODEProblem(logistic!, [N0], tspan)\ntrue_sol = OrdinaryDiffEq.solve(true_prob, Tsit5(); saveat=data_times)\ntrue_N = [true_sol(t)[1] for t in data_times]\nobserved_N = true_N .+ 0.3 .* randn(length(data_times))\nobserved_N = max.(observed_N, 0.01)\n\n# Define PSM: dN/dt = r(N) * N, where r(N) is unknown\nfunction growth!(du, u, p, t)\n    N = u[1]\n    du[1] = p.r(N) * N\nend\n\n# Approximate r(N) with a penalized cubic B-spline (8 knots)\napprox_r = BSplineApproximator(:r, (0.0, 12.0), 8; initial = x -> 0.3)\n\n# Build problem and fit with LAML\nprob = PSMProblem(\n    growth!, [N0], tspan, [approx_r];\n    data_times = data_times,\n    data_values = reshape(observed_N, :, 1),\n    obs_to_state = [1],\n    likelihood = Gaussian(),\n    solver = Tsit5()\n)\nsol = solve(prob, LAML())\n\n# Evaluate the recovered unknown function\nr_fitted = sol.unknown_functions[:r]\nfor N in [1.0, 5.0, 9.0]\n    println(\"r($N): estimated=$(round(r_fitted(N), digits=3)), true=$(round(r0*(1-N/K), digits=3))\")\nend","title":"Quick Start"},{"category":"section","location":"#Package-Features","page":"Home","text":"","title":"Package Features"},{"category":"section","location":"#Solvers","page":"Home","text":"Solver Method ODE-free? Bayesian? Reference\nLAML Penalized IRLS + LAML smoothing No No Wood et al. (2016)\nGCVSolver Penalized IRLS + GCV smoothing No No Wood (2001)\nCollocationLAML Generalized profiling No No Ramsay et al. (2007)\nGradientMatching Smooth then match derivatives Yes No —\nTwoStageSolver Smooth then match (simple) Yes No Varah (1982)\nBNGSolver Ensemble Bayesian gradient matching Yes Yes Bonnaffé & Coulson (2023)\nAdaptiveGradientMatching GP product-of-experts Yes No Macdonald & Husmeier (2015)\nAdamSolver Adam through ODE (UDE-style) No No Rackauckas et al. (2020)\nMultipleShootingSolver Multiple shooting + Adam No No Turan & Jäschke (2021)\nDerivativeFreeSolver NelderMead / Particle Swarm No No —\nRodeoSolver Probabilistic ODE (Kalman) No No Wu & Lysy (2024); Tronarp et al. (2022) for :fenrir\nDaltonSolver Data-adaptive Kalman likelihood No No Wu & Lysy (2024)\nMCMCSolver HMC/NUTS posterior sampling No Yes —\nMagiSolver Manifold-constrained GP inference No Yes Yang et al. (2021)\nPseudoMarginalSolver Pseudo-marginal MCMC (probabilistic ODE likelihood) No Yes Andrieu & Roberts (2009)\nVariationalSolver Mean-field variational inference No Yes —\nABCSolver ABC-SMC (likelihood-free) No Yes —\nIntegralMatchingSolver Integral matching (smooth, then match integrals) Yes No Dattner & Klaassen (2015)\nProfileLikelihoodSolver Profile likelihood identifiability/CIs No No Simpson & Maclaren (2023)\nEnsembleKalmanSolver Ensemble Kalman inversion No No Iglesias et al. (2013)\nODINSolver ODIN-style Mahalanobis gradient matching Yes No Wenk et al. (2020)\nRKHSSolver Trajectory-in-RKHS gradient matching Yes No González et al. (2014)","title":"Solvers"},{"category":"section","location":"#Approximators","page":"Home","text":"Approximator Description Parameters\nBSplineApproximator Cubic B-spline basis Spline coefficients\nShapeConstrainedBSplineApproximator SCOP-spline (Pya & Wood 2015) Constrained coefficients\nSPDEApproximator Matérn SPDE penalty (Lindgren et al. 2011) Mesh node values\nShapeConstrainedSPDEApproximator SPDE + shape constraints Constrained mesh values\nNeuralApproximator Lux.jl neural network Network weights\nGPApproximator Gaussian process GP hyperparameters\nCOMONetApproximator Constrained monotone network exp(W) weights","title":"Approximators"},{"category":"section","location":"#Likelihoods","page":"Home","text":"Gaussian — Gaussian errors with identity link (default)\nPoisson — Count data, fitted on the response scale (identity link)\nNegativeBinomial — Overdispersed counts with fixed dispersion θ, identity link\nTruncatedNormal — Continuous data bounded below (e.g. non-negative densities)\nCustomLikelihood — User-defined likelihood\n\nSee the Getting Started guide for a detailed tutorial, or browse the Solvers and Approximators pages for full documentation. The Vignettes page has 35 worked examples covering every solver and approximator.","title":"Likelihoods"}]
}
